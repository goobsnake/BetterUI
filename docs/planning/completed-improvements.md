# Completed Improvements

## [Completed] BUI-STAB-001 — scene-transition, keybind, and builog stability hardening (2026-07-09)
**Summary**: Hardened scene and input ownership across Banking, CIM lifecycle helpers, Vendor, Trading House, and Inventory. Lifecycle teardown now removes the immutable SHOWING snapshot, handles direct-HIDDEN safely, re-cancels deferred work, deduplicates aliased list cleanup, and unregisters Vendor preview callbacks on every close path. Banking owns personal and guild scene-entry keybinds exactly once and generation-binds hidden-scene recovery. Vendor keeps one forced scene-entry reclaim; Vendor and Trading House coalesce only state-equivalent same-frame global keybind refreshes, with fingerprints covering label-relevant selection, multi-select, inline-buy, guild, search, cooldown, and paging state. Trading House guild changes retain one explicit listings request, while money updates refresh footer/keybind state without rebuilding row data. Builog now gates TraceEvent, SceneLog, Inventory ownership enrichment, and ListRefresh trace-only probes before expensive work; hostile payload metamethods cannot suppress WARN/ERROR/TraceEvent.
**Review/host adjudication**: Independent architect, reviewer, security-auditor, and test-engineer lanes reviewed lifecycle/Banking, Vendor/Trading House, and diagnostics/Inventory slices. Valid findings added exact Inventory probe gates, metatable-independent log copies, personal/guild ordered ownership tests, same-row Vendor fingerprint inputs, explicit-false trace fields, and money-update/listings request ownership coverage. Host rejected stale or false premises: current Vendor core ownership already guards both search flags, and ListingsComponent.BuildList does not request server data. Host also repaired two delegate-created fixture defects before acceptance.
**Source Issue/Phase**: BUI-STAB-001 Phases 1-8 from `project-improvements.md`; ESOUI API/source audit 2026-07-09.
**Related Files/Tests**: `Modules/{Banking,CIM,Inventory,Vendor,TradingHouse}/**/*.lua` lifecycle/keybind/diagnostics slices; new `tools/tests/test_banking_scene_recovery.lua` and `test_vendor_scene_cleanup_once.lua`; strengthened lifecycle, Banking operation-count, Vendor/Trading House coalescing, Log/SceneLog, Inventory probe-count, and list-refresh tests. Host validation: all targeted phase matrices green, full `run_all_tests.lua` aggregate green, and `luac_syntax` 460/460. Scoped `lua_diagnostics` could not provide a valid file-limited result because test-runner ignored `files`, reported `fileCount: 0`, and failed on unrelated workspace warnings; fallback syntax/runtime gates passed and the durable tool defect is logged as ZFF PB-188.

## [Completed] ACC-010 source phases 1-3 + Vendor/Banking/Inventory acceptance — gamepad screen-narration trigger wiring (2026-07-06)
**Summary**: Implemented the minimal `QueueCustomEntry` trigger path for BetterUI's custom screen narration. Added guarded `BETTERUI.CIM.Narration.QueueSceneNarration`, registered custom narration as `NARRATION_TYPE_UI_SCREEN`, queued narration from selected-data changes in Banking, Vendor, Trading House, Companions, Inventory, backpack item-list, and craft-bag paths, and folded selected search item details into ESO's supported `resultsNarrationFunction`. Fixed the Inventory follow-up regression by registering and queueing through `ZO_GAMEPAD_INVENTORY_SCENE_NAME` / `BETTERUI.Inventory.GetNarrationSceneName()` instead of the obsolete `gamepadInventory` alias. User in-game acceptance confirmed Vendor, Banking, and Inventory now narrate per item scrolled; Trading House and Companions remain as the only active manual acceptance checks.
**Source Issue/Phase**: ACC-010 (`project-improvements.md`; detailed plan in `accessibility-narration-plan.md`)
**Related Files/Tests**: `Modules/CIM/Core/Integration/NarrationHelper.lua`, `Modules/CIM/Core/Data/SearchManager.lua`, `Modules/{Banking,Vendor,TradingHouse,Companions,Inventory}/**/*.lua` narration call sites, `tools/tests/{test_accessibility_narration_source,test_narration_callbacks,test_registry_dependency_contract_source}.lua`, `docs/publishing/changelog.txt`. Validation: focused narration source test green; touched-file `luac_syntax` green; full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green; local Live AddOns deploy via `tools/Update_BetterUI.ps1` succeeded.

## [Completed] BUI-TRACE-002B — monitor input/combat/digest closeout via static verification + hot-path fix (2026-07-03)
**Summary**: Closed via host code spot-checks per maintainer instruction. Keybind traces: `CIM/Keybinds/InputAnchor.lua` gates with `enabledFor` before payload construction, expensive `binding` field behind a second trace-level gate — SOUND. Anomaly: `Watchdog.lua` sweep is a 1 s self-stopping timer with `isActive`/`pendingCount` guards — SOUND. Digest/snapshot: `WatchMode.Snapshot()` preflights `EnabledFor(DEBUG, STATE)` before building data or invoking providers; heartbeat timer-driven — SOUND. Combat/HUD: FIXED a real defect — three per-tick `table.concat` dedup-key builders ran ungated on hot paths (`FrontBarCooldowns` quickslot-count ~60 fps, `FrontBarManager` usability 100 ms × slots, `UltimateManager` meter 100 ms); each now sits behind an EnabledFor preflight (`ShouldTraceFrontCooldown`/`ShouldTraceFrontBar`/`ShouldTraceUltimate`) so the concat only runs while tracing is active, mirroring the already-gated transition tracers.
**Source Issue/Phase**: BUI-TRACE-002B (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Keybinds/InputAnchor.lua`, `Modules/CIM/Core/Diagnostics/{Watchdog,WatchMode}.lua`, `Modules/ResourceOrbFrames/SkillBar/{FrontBarCooldowns,FrontBarManager,UltimateManager}.lua`. Full suite + log-message lint green.

## [Completed] BUI-CONS-002 — tracer migration to Log.MakeTracer: final disposition (2026-07-03)
**Summary**: Closed with the CIM/ROF bulk slice resolved as KEEP-by-design after full enumeration. Within CIM + Banking/Companions there are no byte-duplicate tracer wrappers left (the cross-file duplicates were consolidated in wave 1); the remaining single-file `local function TraceX(...)` wrappers reference `BETTERUI.Log` at call time and are load-safe, whereas converting them to `local TraceX = MakeTracer{...}` is a load-time cross-namespace capture: eight test harnesses dofile those files with partial `BETTERUI.Log` stubs lacking `MakeTracer`, so the guarded conversion idiom would silently no-op the tracers those tests capture. Low dedup value, real regression risk — the hand-rolled wrappers stay. Any future migration should be a dedicated slice with per-file harness updates.
**Source Issue/Phase**: BUI-CONS-002 final task (`project-improvements.md`); module slices archived separately under wave 1.
**Related Files/Tests**: evaluation-only; no code changed.

## [Completed] BUI-CONS-006 — CIM numeric-entry dialog builder: evaluated and rejected (2026-07-03)
**Summary**: Closed as evaluated-and-rejected via the Banking QuantityDialog pilot analysis. The dialog's confirm path interleaves generic numeric guards with Banking-specific logic (CIM slot-identity re-validation, live `GetSlotStackSize` re-check, list-proxy `MoveItem`, window `_suppressListUpdates` writes), and `test_banking_quantity_dialog.lua` pins the exact descriptor shape plus eight distinct close-path behaviors. A helper generic enough to share would be a pass-through with a leaky, wide callback interface — net-negative indirection rather than dedup. Decision: keep the per-module dialogs. If ever revisited, candidate sites are `Inventory/Dialogs/{CraftBagQuantityDialog,InventoryDialogs}.lua`, `Banking/Currency/CurrencySelector.lua`, `TradingHouse/Core/{PriceEntry,TradingHouseRuntimeFlow}.lua`.
**Source Issue/Phase**: BUI-CONS-006 (`project-improvements.md`)
**Related Files/Tests**: evaluation-only; no code changed.

## [Completed] BUI-CONS-010 — WatchMode snapshot keybind-present helper (2026-07-03)
**Summary**: Added `WatchMode.KeybindPresent(descriptor)` (guarded `BETTERUI.Interface.HasKeybindGroup(descriptor) and 1 or 0`) to `Modules/CIM/Core/Diagnostics/WatchMode.lua` and rewired the five per-module `XxxSnapshotKeybindPresent` copies (Banking, Companions, Inventory, TradingHouse, Vendor) as call-time forwarders. Snapshot digest output byte-identical. A `KeybindPresent` unit block was added to `test_watchmode.lua`. Completes BUI-CONS-010 (tasks 1 and 3 were archived under wave 1).
**Source Issue/Phase**: BUI-CONS-010 task 2 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/WatchMode.lua`, `Modules/{Banking/Banking,Companions/Module,Inventory/Inventory,TradingHouse/TradingHouse,Vendor/Vendor}.lua`, `tools/tests/test_watchmode.lua`.

## [Completed] BUI-CLEAN-001 — dormant performance-instrumentation pair retired (2026-07-03)
**Summary**: Decision: retire. Verified both systems were production-dead (only `DebugCommands.lua` referenced `BETTERUI.CIM.Profiler`; nothing called `Perf.Begin/End/Measure` since the 9f8ce2aa dead-code pass removed its last consumer). Deleted `Modules/CIM/Core/Diagnostics/{Perf,PerformanceProfiler}.lua`, the `[Performance Profiler]` memory-dump block, the `/buiprofile` slash command + help line in `DebugCommands.lua`, both manifest lines, the stale README bullets, the profiler globals from `.luarc.json`, and `tools/tests/{test_perf,test_performance_profiler}.lua`.
**Source Issue/Phase**: BUI-CLEAN-001 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/{DebugCommands.lua,README.md}`, `BetterUI.txt`, `.luarc.json`. Full suite green after removal.

## [Completed] BUI-CLEAN-002 — test-pinned production-dead API register fully drained (2026-07-03)
**Summary**: Final slice: removed `ControlCache.Create`/`CacheChildren` (the file's live surface, `CacheButtonChildren`, retained for ControlUtils + skill-bar consumers), `Defaults.IsDestructive`/`GetDefault` plus the write-only `DestructiveSettings` table (its only reader was the dead accessor), and `ListRefreshManager:IsDirty` (the `isDirty` field and live `MarkDirty` writer remain). Pinning tests rewritten to negative stays-removed assertions (`test_control_cache.lua`, `test_defaults_registry.lua`, `test_cim_list_support_source.lua`). With the wave-1 slice this closes the register; the only deliberate keeper remains `Class:BatchDeposit` (documented under the wave-1 entry) and the exempt `_ForTests` seams.
**Source Issue/Phase**: BUI-CLEAN-002 final task (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Window/ControlCache.lua`, `Modules/CIM/Core/Settings/DefaultsRegistry.lua`, `Modules/CIM/Lists/ListRefreshManager.lua`, updated tests above. Full suite green.

## [Completed] FR checkpoint PLT-003 — uniqueId position restore closed via host spot-check (2026-07-03)
**Summary**: Verified statically: `PositionManager` SavePosition/RestorePosition round-trip is symmetric (`{index, uniqueId}` written and read with the same `dataSource or self` unwrap on both sides); uniqueId match is preferred with index as clamped fallback only; `GenericWindow:SwitchToCategory` saves the outgoing position, rebuilds via `RefreshList()`, then restores against the live post-rebuild `dataList` with `zo_clamp` bounds. No defect found.
**Source Issue/Phase**: `feature-requests.md` → In-Game Validation Checkpoint 1 (PLT-003)
**Related Files/Tests**: `Modules/CIM/Core/Window/GenericWindow.lua`, `Modules/CIM/Core/Data/PositionManager.lua`.

## [Completed] PB-017 — vendor buy-list retry single owner (2026-07-03)
**Summary**: Resolved the dual-owner retry conflict found in the dead-code review. `VendorControllerRuntime` is now the single lifecycle-scoped owner of the `"buyListRetry"` task and `instance._buyListRetryCount` counter (limit 20 at 180 ms); `BuyComponent:BuildList` no longer schedules its own competing retry (old limit 3 at 80 ms×n), so an empty buy list retries with one policy and one counter. Added a regression test asserting `Buy:BuildList` with zero store rows schedules no `buyListRetry` task and leaves `_buyListRetryCount` untouched.
**Source Issue/Phase**: PB-017 (`priority-backlog.md`)
**Related Files/Tests**: `Modules/Vendor/Components/BuyComponent.lua`, `Modules/Vendor/Core/Lifecycle/VendorControllerRuntime.lua`, `tools/tests/test_vendor_buy_primary_enablement.lua`, `tools/tests/test_vendor_list_runtime.lua`. Commit `4e773a50`; full suite 216/216 green.

## [Completed] BUI-CONS-001 — CIM SafeGetTargetData adopted across Vendor/TradingHouse components (2026-07-03)
**Summary**: Replaced the ten byte-identical local `GetTargetRowData` helpers (7 Vendor components, 3 TradingHouse components) plus the inline copy in `TradingHouseRuntime.lua` with the canonical `BETTERUI.CIM.Utils.GetListTargetData or SafeGetTargetData` resolution already proven by `BuyComponent`. Test harnesses that dofile components without CIM received faithful `SafeGetTargetData` stubs.
**Source Issue/Phase**: BUI-CONS-001 (`project-improvements.md`)
**Related Files/Tests**: `Modules/Vendor/Components/*.lua`, `Modules/TradingHouse/Components/{Sell,Listings,Browse}Component.lua`, `Modules/TradingHouse/Core/TradingHouseRuntime.lua`, `tools/tests/test_trading_house_sell_filter.lua` + vendor component tests. Commit `4e773a50`.

## [Completed] BUI-CONS-002 (partial) — module-slice tracer migration to Log.MakeTracer (2026-07-03)
**Summary**: Migrated the module-owned tracer slices to `BETTERUI.Log.MakeTracer`: the 4 identical ROF `TraceDrag` copies now live once in `ROF/Core/Utils.lua`; GeneralInterface `TraceGeneralSetting` canonicalized in `SettingsHelpers.lua` with `CraftingPriceTooltip`/`Tooltips` on full MakeTracer; Writs tracer/state-envelope helpers owned by `Core/Writ.lua` (`Writs.Trace`, `Writs.TraceWritState`) with call-time-guarded Module.lua delegators; Nameplates and Companions on MakeTracer bases with thin wrappers preserving per-event category routing and unconditional `SetLastAction` where the originals had it; TradingHouse leaf tracers (`TraceTHRuntime`, `TraceFilters`, `TracePresets`) migrated. Ruled non-migratable and kept: the TH `TraceTH` family (instance payload enrichment + pinned signatures) and GI `TraceGeneralInterface` (`_inputAnchorDetail` branch). Known accepted nuance: full-MakeTracer drop-ins gate `SetLastAction` behind trace enablement, matching already-shipped Banking/Vendor behavior. The CIM/ROF bulk slice remains active.
**Source Issue/Phase**: BUI-CONS-002 tasks 1–3 (`project-improvements.md`)
**Related Files/Tests**: ROF/GeneralInterface/Writs/Nameplates/Companions module files, `tools/tests/test_writs_trace_source.lua`, `tools/tests/test_builog_flow_source.lua`. Commit `4e773a50`.

## [Completed] BUI-CONS-003 — GetCurrentSceneName dedup (2026-07-03)
**Summary**: Deleted the 13+ local reimplementations of the `SCENE_MANAGER:GetCurrentSceneName()` guard across Nameplates, GeneralInterface, Companions, and ResourceOrbFrames; callers now use `BETTERUI.CIM.Utils.GetCurrentSceneName()` (ROF via a `ROF/Core/Utils.lua` re-export to keep its CIM surface narrow). ROF call sites use call-time guarded forwarders so test harnesses with partial Utils stubs stay loadable.
**Source Issue/Phase**: BUI-CONS-003 (`project-improvements.md`)
**Related Files/Tests**: `Modules/Nameplates/*`, `Modules/GeneralInterface/*`, `Modules/Companions/Core/CompanionItemList.lua`, `Modules/ResourceOrbFrames/{ResourceOrbFrames.lua, Core/OrbEvents.lua, Core/Utils.lua, SkillBar/FrontBarCooldowns.lua}`. Commit `4e773a50`.

## [Completed] BUI-CONS-004 — dead load-order fallback shims retired (2026-07-03)
**Summary**: Collapsed the unreachable `type(fn) == "function"` fallback re-implementations: Vendor `ExecuteSafely` is a thin delegator to `CIM.SafeExecute` with the `DefaultSafeCall` else-branch, `PackResults`, and keybind pcall fallbacks removed; Banking's local wrappers (`ReadTransferContextSnapshot`, `IsActionableBankSlotEntry`, six KeybindManager wrappers, `GetBankingWindow` ×3, duplicate `RegisterBankingWatchScenes`) deleted with call sites on canonical `BETTERUI.Banking.*`/`BETTERUI.Interface.*` helpers; the "prefer GetCurrentCategoryKey() else walk bankCategories" ladder folded into a new canonical `BETTERUI.Banking.ResolveWindowCategoryKey(window)` across all six copies (BankingActions ×2, TransferActions, BankingClass RefreshWindowViewNow, StateManager ToggleList, BankingSceneLifecycle RebuildCategoriesAndRefreshList); Nameplates reset paths routed through `Positioning.ResetOffsets` with Clamp fallbacks deleted and the dead LAM `FireCallbacks` shim removed.
**Source Issue/Phase**: BUI-CONS-004 (`project-improvements.md`)
**Related Files/Tests**: Banking module files, `Modules/Vendor/Core/{VendorSafeExecute,VendorKeybinds}.lua`, `Modules/Vendor/Core/Lifecycle/VendorInteractionRuntime.lua`, `Modules/Nameplates/{Settings,Positioning}.lua`, banking/vendor source-pin and runtime tests. Commit `4e773a50`.

## [Completed] BUI-CONS-005 — shared secure-action-failed notifier (2026-07-03)
**Summary**: Added `BETTERUI.CIM.UserNotifySecureActionFailed(context)` beside `CIM.UserNotify` in `Modules/CIM/Core/Diagnostics/SafeExecute.lua` and replaced the 3 identical local definitions + 4 inline copies across Inventory (EquipAction, CraftBagKeybinds, Module, ItemActionHandlers, InventoryDialogs).
**Source Issue/Phase**: BUI-CONS-005 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/SafeExecute.lua`, Inventory action/keybind/dialog files. Commit `4e773a50`.

## [Completed] BUI-CONS-007 — CIM dialog register-with-prior-chaining helper (2026-07-03)
**Summary**: Added `BETTERUI.CIM.Dialogs.RegisterWithPriorChain(dialogName, dialogInfo, options)` (prior-info read, prior/own setup chaining with nil-inner-setup guard, `{overwrite=true}` default, returns `(registered, priorDialog)`) to `DialogRegistry.lua` and deleted the 12 copied `GetCurrentDialogInfo`/`ChainPriorDialogSetup`/`RegisterXxxDialog` helpers across TradingHouse `PriceEntry`, `SearchPresets`, `BrowseFilters`, and `TradingHouseRuntimeFlow`.
**Source Issue/Phase**: BUI-CONS-007 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Dialogs/DialogRegistry.lua`, TradingHouse core files, `tools/tests/test_trading_house_search_presets.lua`. Commit `4e773a50`.

## [Completed] BUI-CONS-008 — Vendor component store-interaction skeleton (2026-07-03)
**Summary**: Extracted the shared Vendor helpers into `Core/Policy/VendorModePolicy.lua`: `Vendor.AddItemRow(list, entryData)`, `Vendor.DispatchTracedAction(event, traceData, fn)` (byte-identical `TraceActionRequested → fn() → ScheduleActionSettled` envelope with `goldBefore` threading), `Vendor.AuthorizeAction(mode, ds, instance)`, `Vendor.CanAffordStoreEntry(instance, ds)`, `Vendor.PerRefreshCache(computeFn)`, `Vendor.IsAtGoldCap()`, and the SellVengeance availability predicate (canonical in `Vendor.lua`, delegated from the component and native-store bridge). All components plus `VendorBatchRuntime` (BUY-branch affordability, `AuthorizeVendorInventoryAction`, `IsAtGoldCap`) rewired. The `vendor.repair_all`/`vendor.stable_train` envelopes were intentionally left un-migrated (distinct phase semantics). Accepted minor ordering note: FenceSell `SetIconDesaturation` now runs after `AddEntry` (equivalent for reference-based ZO lists).
**Source Issue/Phase**: BUI-CONS-008 (`project-improvements.md`)
**Related Files/Tests**: `Modules/Vendor/Core/Policy/VendorModePolicy.lua`, all `Modules/Vendor/Components/*.lua`, `Modules/Vendor/Core/{VendorBatchRuntime,List/BatchActionCounts}.lua`, vendor test suite incl. `test_vendor_action_outcomes.lua`, `test_vendor_authorization_surface_source.lua`. Commit `4e773a50`.

## [Completed] BUI-CONS-009 — Inventory duplication cluster (2026-07-03)
**Summary**: Merged the two byte-identical destroy-confirm blocks behind `HandleDestroyConfirmForTarget(self, targetData, source)`; unified the equality/quest predicates on hardened `BETTERUI.Inventory.Utils.MenuEntryTemplateEquality` (nil-safe/id64-hardened — deliberately NOT the naive CIM export, which would have regressed id64 comparison) and `HasQuestItemMarkers` (base predicate shared; per-module native-filter fallbacks retained); extracted the duplicated 120-retry/50 ms dialog-restore loop + trace helper into new `Modules/CIM/Dialogs/DialogRestore.lua` (`DialogRestore.Log`, `DialogRestore.Schedule` with per-key unique task names) consumed by `InventoryClass` and `ItemActionHandlers` via call-time-guarded forwarders; deduped the gamepad sort schema (`BuildGamepadItemSort`), parameterized the PositionManager craft-bag/inventory branches by field names, routed `ShowCraftBagBatchActionsMenu` through `MultiSelectMixin.CreateDialogEntry`, unified the filter comparator on the pcall-guarded class method, exported `STOCK_TOOLTIP_BODY_FONT`/`RestoreStockLabelFonts` from TooltipUtils, and removed the superseded singular craft-bag count getter. `CraftList:GetFilterComparator` (table-of-filters variant) left as-is — not equivalent.
**Source Issue/Phase**: BUI-CONS-009 (`project-improvements.md`)
**Related Files/Tests**: Inventory core/list/action/UI files, `Modules/CIM/Dialogs/DialogRestore.lua`, `BetterUI.txt` (DialogRestore manifest line), inventory test suite. Commit `4e773a50`.

## [Completed] BUI-CONS-010 (partial) — Banking/Writs/Companions small-helper dedup (2026-07-03)
**Summary**: Added `BETTERUI.Banking.GetWindow()` and `BETTERUI.Banking.RegisterWatchScenes(watch)` (replacing 3 `GetBankingWindow` copies and 2 `RegisterBankingWatchScenes` copies); moved the Writs count/visibility/trace helpers onto `BETTERUI.Writs` owned by `Core/Writ.lua` and reused from `Module.lua`; deduped the Companions category-title computation (`ComputeCategoryTitle`) and keybind wrap helper (`Companions.WrapKeybindGroup`). The `WatchMode.KeybindPresent` snapshot-provider slice remains active.
**Source Issue/Phase**: BUI-CONS-010 tasks 1 and 3 (`project-improvements.md`)
**Related Files/Tests**: Banking/Writs/Companions module files, `tools/tests/test_builog_flow_source.lua`. Commit `4e773a50`.

## [Completed] BUI-CONS-011 — ResourceOrbFrames skill-bar shared cooldown text style (2026-07-03)
**Summary**: Moved `ApplyCooldownTextStyle(label, textSize, color, applyFont)` into `SkillBar/CooldownUtils.lua` (latched styling; `applyFont` lets the front bar's fixed-font alt-timer opt out); both bars consume it through call-time forwarders so partial test-harness stubs stay loadable. The parameterized `SharedBarButtons` cache/refresh helper was evaluated and NOT implemented — parity is not provable: the bars' button-cache loops (index-keyed vs mapping-list + optional quickslot/companion buttons) and overlay-refresh loops (quickslot empty-state/dual-timer vs single `cooldownText` + `isGamePad` branch) genuinely diverge, per the entry's own "implement only if parity is provable" condition.
**Source Issue/Phase**: BUI-CONS-011 (`project-improvements.md`)
**Related Files/Tests**: `Modules/ResourceOrbFrames/SkillBar/{CooldownUtils,FrontBarCooldowns,BackBarManager}.lua`, `tools/tests/{test_front_bar_cooldown_restart,test_back_bar_manager,test_cooldown_utils}.lua`. Commit `4e773a50`.

## [Completed] BUI-CLEAN-002 (partial) — wave-1 drain of the test-pinned production-dead API register (2026-07-03)
**Summary**: Removed with pin updates: `Vendor.ClampPurchaseQuantity`, `BETTERUI.Inventory.SlotActionsVisibilityHelpers`, `TH.GetTabs`, `Filters.BuildNameHashFilter`, `VendorClass:SuppressListUpdates`, `TradingHouseClass:SuppressListUpdates/FlushListUpdates` plus the entire now-inert TH `_suppressListUpdates`/`_isDirty` mechanism (guard, onHiding resets, snapshot fields — its only true-writer was the removed method, so the suppression branch was unreachable; Banking's same-named mechanism is live and untouched), `GetCategoryTypeFromWeaponType`, the singular `GetCraftBagCategoryItemCount`, `Drag.SetElementLocked/GetOffset`, `CooldownUtils.GetCooldownVisualArmingGeneration` (internal arming counter retained), and the `SI_BETTERUI_BUILOG_POPUPS(_TOOLTIP)` + `SI_BETTERUI_RESOURCE_ORB_FRAMES_ORB_OFFSET_X/Y(_TOOLTIP)` lang keys (42 lines across 7 locales). Wired instead of removed: `PriceEntry.ShouldOfferDigitEntry` now gates the create-listing digit-price row (inserted at index 3 only when the gate passes). Kept by design: `Class:BatchDeposit` (`test_batch_safety.lua` is built around it). Remaining slice (ControlCache/Defaults/ListRefreshManager accessors) stays active.
**Source Issue/Phase**: BUI-CLEAN-002 (`project-improvements.md`)
**Related Files/Tests**: Vendor/TradingHouse/Inventory/ROF module files, `lang/*.lua`, `.luarc.json`, pin updates across ~12 test files. Commit `4e773a50`.

## [Completed] BUI-REGR-L4-VERIFY-001A–D — live-verification items closed via host code spot-checks (2026-07-03)
**Summary**: Per maintainer instruction, the remaining in-game (L4) verification tasks were closed by targeted host code spot-checks with corrections applied where defects were found. Results: (A) guild-bank redirect emits on every path with no silent branch, and the dead-interaction rejection is re-evaluated inside the deferred fallback — SOUND (redirect phases additionally migrated to the canonical vocabulary: `skipped`/`requested`+path/`fired`+trigger); guild-trader handoff covers every bail path with `trading_house.scene` + `trading_house.native_handoff` traces including failed-open recovery — SOUND. (B) stablemaster flow guards double-open and mirrors store-event phases — SOUND; vendor search focus is preserved through refreshes (`_preserveSearchFocusDuringRefresh`), and sort/search exits restore exactly the removed keybind groups plus a reentrancy-guarded `RefreshVendorHeader` — SOUND. (C) default-tooltip native top area is preserved unless BetterUI owns the status label (`_betterUiStatusOwned` set/cleared only on owned paths) and enhanced→equipped→non-equipped leaves no stale header — SOUND; quickslot default `x=270/y=-48` is a code constant plus additive offset, un-clobberable by migrations — SOUND. (D) `nameplates.init` traces emit through the gated MakeTracer base with clean flow attribution, and settings-control traces stay demoted behind TRACE — SOUND; compass/reticle repositioning reapplies on activation and gamepad-mode change, `EnsureHandle` is idempotent (no duplicate drag handles), and disable restores native anchors — SOUND.
**Source Issue/Phase**: BUI-REGR-L4-VERIFY-001A/B/C/D (`project-improvements.md`)
**Related Files/Tests**: `Modules/Banking/Banking.lua`, `Modules/TradingHouse/Core/TradingHouseRuntimeFlow.lua`, `Modules/Vendor/{Vendor.lua, Core/VendorClass.lua}`, `Modules/Inventory/UI/{TooltipEquipped,TooltipUtils}.lua`, `Modules/ResourceOrbFrames/Constants.lua`, `Modules/Nameplates/{Nameplates,Positioning}.lua`. Commit `4e773a50`.

## [Completed] BUI-TRACE-002A — monitor core-flow closeout via static trace-chain verification (2026-07-03)
**Summary**: Closed per maintainer instruction by static verification of the monitored flows instead of a live `/builog preset inspect` session. The bank-transfer chain was traced end-to-end at exact emit sites: `FlowBegin("bankTransfer")` → `requested` (`TransferActions.lua:278`) → `confirmed` on the three settle paths (source-empty, single-slot update, guild slot update) → `list.refresh` queued/saved/executed/restore_end in `ListRefreshManager` with `flow` threaded via `RefreshWindowView → QueueRefresh`; consumer wiring (event registrations + `InitializeRefreshManager`) confirmed live. Vendor `requested→settled` with `goldBefore/goldAfter/goldDelta` is enforced by `test_vendor_action_outcomes.lua` source pins (now covering the shared `DispatchTracedAction` envelope). Trading House `requested→completed` with shared `opId`, `settings.value write_before/write_after`, and privacy scrubbing remain pinned by `test_builog_flow_source.lua`.
**Source Issue/Phase**: BUI-TRACE-002A (`project-improvements.md`)
**Related Files/Tests**: `Modules/Banking/Actions/TransferActions.lua`, `Modules/CIM/Lists/ListRefreshManager.lua`, `tools/tests/{test_builog_flow_source,test_vendor_action_outcomes}.lua`. Commit `4e773a50`.

## [Completed] FR in-game validation checkpoints PLT-006 / INV-003 / TRC-002 / TRC-003 / TRC-004 / TRC-005 — closed via host spot-checks (2026-07-03)
**Summary**: Per maintainer instruction, the 2026-06-19 first-cut in-game checkpoints (except PLT-003, still pending) were closed by code spot-checks with fixes applied where needed. PLT-006: the Companions equip scene wires `CIM.Narration.RegisterListNarration` behind an existence guard; remaining HUD-overlay modules expose no list scenes needing narration — wiring complete for applicable scenes. INV-003: companion item preview is guarded by `CanPreviewCompanionItem`, prefers the `itemPreview` SYSTEM with engine fallback, and activation is balanced by `EndCompanionItemPreview` on action end and scene `onHiding`; keybind label uses native `SI_ITEM_ACTION_PREVIEW`. TRC-002: decision recorded — single-item buy default retained and the unwired `ClampPurchaseQuantity` seam removed as dead (BUI-CLEAN-002); a quantity spinner is a new-feature ask if ever wanted, not a validation item. TRC-003: the browse name filter applies on first search — `ApplyFilters` pushes `SetSearchText` and `DoSearchWhenReady` defers dispatch until `CanPerformSearch()` with a 10 s timeout fallback. TRC-004: digit price entry verified AND FIXED — the `ZO_CurrencySelector_Gamepad` selector leaked input capture on dialog close; a `finishedCallback` teardown now deactivates `dialogData._priceSelector` (parity with the create-listing `_activeSlider` teardown); the digit row is additionally gated by `ShouldOfferDigitEntry`. TRC-005: stable active-mount icon resolves via `GetActiveCollectibleByType → GetCollectibleIcon` with a default-icon fallback, and the no-mount warning alerts with a localized string + English fallback (minor known nit: the warning fires on every Activate rather than once per tab).
**Source Issue/Phase**: `feature-requests.md` → In-Game Validation Checkpoints 2–7
**Related Files/Tests**: `Modules/Companions/{Core/CompanionsRuntime.lua, Actions/CompanionActions.lua}`, `Modules/TradingHouse/Core/{PriceEntry.lua, TradingHouseRuntimeFlow.lua, BrowseFilters.lua}`, `Modules/Vendor/Components/StableTrainingComponent.lua`. Commit `4e773a50`.

## [Completed] BUI-CLEAN-002 (partial) — Vendor dead confirmationMode/spinner read-path retirement (2026-07-03)
**Summary**: Retired Vendor's dead `confirmationMode`/`.spinner` read paths after adversarial verification distinguished the two `.spinner` families: the removed BetterUI instance spinner (always nil) versus the live native `STORE_WINDOW_GAMEPAD.spinner` (verified against ESOUI `storewindow_gamepad.lua:19`). Removed three always-true/always-false `confirmationMode` terms in `VendorClass.lua` (deferred-normalization guard, directional-ownership branch, list-input guard), the always-nil `instance.spinner` release call in `VendorControllerRuntime.ForceReleaseDirectionalInput`, and the two always-nil `Vendor.spinner` probes from the `/buidebug` ownership dump (native-store probes retained). Deliberately KEPT (live native-spinner paths): `ReleaseSpinnerDirectionalInput`, the `Allow` helper's generic spinner guards, `VendorControllerRuntime.lua` storeManager release, `Vendor/Module.lua` generic release, and `DebugCommands` NativeStore probes. Updated `test_vendor_tabs.lua` mirror harness for parity and trimmed stale WindowClass spinner doc remnants.
**Source Issue/Phase**: BUI-CLEAN-002 Vendor follow-up task (`project-improvements.md`)
**Related Files/Tests**: `Modules/Vendor/Core/VendorClass.lua`, `Modules/Vendor/Core/Lifecycle/VendorControllerRuntime.lua`, `Modules/CIM/Core/Diagnostics/DebugCommands.lua`, `Modules/CIM/Core/Window/WindowClass.lua`, `tools/tests/test_vendor_tabs.lua`. Validation: full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green.

## [Completed] BUI-CLEAN-002 (partial) — dead Window spinner subsystem and Banking confirmation-flow removal (2026-07-03)
**Summary**: Removed the fully dead Window spinner subsystem after adversarial verification confirmed no XML creates a `SpinnerContainer` child anymore (its template was deleted as dead in commit aae310aa), so `self.spinner` was permanently nil and every spinner path a no-op. Deleted from `WindowClass.lua`: the spinner lookup/`InitializeSpinner` wiring, `WrapInt`, `ActivateSpinner`, `DeactivateSpinner`, `UpdateSpinnerConfirmation`, `ApplySpinnerMinMax`, and the never-populated `triggerSpinnerBinds`. Removed Banking's unreachable confirmation-mode paths (`CancelWithdrawDeposit` guard, `BankingSceneLifecycle` dead branch, never-assigned `spinnerKeybindStripDescriptor` refs) and the orphaned XML virtuals `BETTERUI_SpinnerButtons`, `BETTERUI_GamepadSpinner`, and `BETTERUI_Header_BrowseResults`, plus the 6 `SI_BETTERUI_BROWSE_*` lang keys (42 lines across 7 locales) whose only consumer was the removed header template. Verified `BETTERUI_GoldSpinnerContainerTemplate` remains live and untouched. Initialize's `DeactivateSpinner()` call was deleted rather than replaced because `GetList()` is nil at that point (verified), so no behavior change.
**Source Issue/Phase**: BUI-CLEAN-002 second task (`project-improvements.md`); remaining BUI-CLEAN-002 scope (test-pinned dead API register + new Vendor confirmationMode/.spinner follow-up) stays active.
**Related Files/Tests**: `Modules/CIM/Core/Window/WindowClass.lua`, `Modules/Banking/{Actions/TransferActions.lua, Scene/BankingSceneLifecycle.lua, State/StateManager.lua}`, `Modules/CIM/Templates/{InterfaceLibrary.xml, GenericHeader.xml}`, `lang/*.lua`, `tools/tests/test_banking_transfer_actions.lua`, `.luarc.json`. Validation: full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green; XML batch validation 12/12 green; zero surviving references confirmed by repo-wide grep.

## [Completed] BUI-REGR-20260703B — vendor header refresh and tooltip status ownership follow-up (2026-07-03)
**Summary**: Closed the follow-up source findings from the July 3 play-test planning review. Added production `BETTERUI.Vendor.Class:RefreshVendorHeader()` so vendor search exit reactivates visible header tab bars and refreshes carousel anchors instead of calling a missing method behind an existence guard. Hardened the search lifecycle test so the production method must exist and the header carousel refresh is observable. Made `_betterUiStatusOwned` functional across enhanced custom status and default native status labels: BetterUI now clears `GAMEPAD_TOOLTIPS` status labels only when it previously claimed ownership, preserving native-painted default status areas while preventing stale "Equipped" headers on equipped-to-non-equipped transitions.
**Source Issue/Phase**: `BUI-REGR-ICONS-002`, `BUI-REGR-TOOLTIP-002` (`project-improvements.md`)
**Related Files/Tests**: `Modules/Vendor/Core/VendorClass.lua`, `Modules/Inventory/UI/TooltipEquipped.lua`, `tools/tests/test_search_lifecycle_contract.lua`, `tools/tests/test_tooltip_helpers.lua`, `docs/planning/project-improvements.md`. Validation: touched-file `test_validate(luac_syntax)` green; focused `test_validate(lua_run)` green for `test_search_lifecycle_contract.lua` and `test_tooltip_helpers.lua`; full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green.

## [Completed] BUI-ENH-NAMEPLATES-001 — compass/reticle name-surface repositioning (2026-07-03)
**Summary**: Researched the requested default UI surfaces with ESOUI API/reference data and implemented the addon-movable portion. `ZO_CompassFrame` and the reticle interaction/non-interaction prompt controls now have persisted Nameplates settings for enable/disable, a shared unlock gate, X/Y sliders, reset positions, and drag handles while unlocked. Disabling Enhanced Nameplates or either mover restores native anchors; player activation and gamepad-mode changes reapply offsets so keyboard/gamepad compass template swaps stay aligned. Engine-rendered overhead world nameplates remain outside Lua control anchoring, so the feature covers the exposed compass/reticle surfaces rather than impossible per-world-nameplate movement.
**Source Issue/Phase**: `BUI-ENH-NAMEPLATES-001` (`project-improvements.md`)
**Related Files/Tests**: `Modules/Nameplates/Positioning.lua`, `Modules/Nameplates/Nameplates.lua`, `Modules/Nameplates/Settings.lua`, `BetterUI.txt`, `Modules/CIM/Core/{Data/Types.lua,Settings/DefaultsRegistry.lua,Settings/SettingsMetadata.lua}`, `lang/*.lua`, architecture docs, and tests `test_nameplates_reset.lua`, `test_nameplates_module_identity_source.lua`, `test_bootstrap_setup_contracts_source.lua`, `test_general_interface_source.lua`, `test_defaults_registry.lua`. Validation: localization missing-key audit green; focused `lua_run` 19/19 green; touched-file `luac_syntax` 52/52 green.

## [Completed] BUI-REGR-20260703 — vendor/bank/store/stable/tooltips/orb-frame remediation (2026-07-03)
**Summary**: Drained the host-fixable project-improvement items from the July 3 play-test review. Guild bank fallback redirects now reject dead interactions before deferred scene shows, and guild-bank scene setup registers early events/category state before native selection while avoiding duplicate late registration. TradingHouse now defaults on for first installs, has a one-time existing-SavedVars enablement migration, and defensively hands failed/no-instance/mismatched BetterUI opens back to the native guild-store flow instead of stranding the interaction. Stablemaster handling proactively opens the BetterUI vendor scene with a stable seed, preserves that seed through nil interaction-type runtime opens, and seeds close cleanup before clearing state. Vendor and Banking search exits gained generation-token cleanup guards; Vendor deferred refreshes preserve active search focus, and search exit re-renders the vendor header/tab strip so merchant category icons do not stay dim after scroll-exiting the search box. Default tooltips preserve ESOUI's native top/equipped area while enhanced-mode status text now uses ownership tracking so stale BetterUI status labels clear only when BetterUI owns them. Quickslot defaults moved left/up (`x=270`, `y=-48`). Resource Orb Frames replaced per-element visible locks and the old "move orbs independently" UI with one global unlock setting; old per-element unlocks and legacy orb offsets migrate into the new model, and element sliders are gated by the global unlock. Builog flow-end handling clears stale ambient flow attribution and high-volume settings-control traces are demoted behind TRACE.
**Source Issue/Phase**: `project-improvements.md` items `BUI-REGR-GB-001`, `BUI-REGR-GS-001`, `BUI-REGR-STABLE-001`, `BUI-REGR-SEARCH-001`, `BUI-REGR-ICONS-001`, `BUI-REGR-TOOLTIP-001`, `BUI-ENH-QUICKSLOT-001`, `BUI-ENH-ORBLOCK-001`, `BUI-BUILOG-002` host-fixable source tasks.
**Related Files/Tests**: Banking (`Banking.lua`, `BankingSceneLifecycle.lua`, `SearchManager.lua`), TradingHouse (`TradingHouseRuntimeFlow.lua`), Vendor (`Vendor.lua`, `VendorClass.lua`, `VendorBootstrapRuntime.lua`, `VendorInteractionRuntime.lua`), Tooltips (`GeneralInterface/Tooltips.lua`, `TooltipEquipped.lua`, `TooltipUtils.lua`), ResourceOrbFrames (`Constants.lua`, `Module.lua`, `Settings/Defaults.lua`, `Settings/SettingsSubmenus.lua`, `Core/ElementDrag.lua`, lang files), diagnostics/settings (`Log.lua`, `SettingsFactory.lua`), and tests (`test_search_lifecycle_contract.lua`, `test_banking_guild_bank.lua`, `test_trading_house_callbacks.lua`, `test_vendor_stable_transition.lua`, `test_tooltip_helpers.lua`, `test_orb_combat_indicators.lua`, `test_resource_orbframes_core_support_source.lua`, `test_resource_orbframes_element_drag.lua`, `test_resource_orbframes_module_defaults.lua`, `test_log.lua`, `test_cim_integration_support_source.lua`, `test_orb_independent_positioning.lua`, `test_bootstrap_orchestration.lua`). Validation: localization missing-key audit green; `test_validate(luac_syntax)` 42/42 green plus final 12/12 green; targeted `test_validate(lua_run)` 15/15 green; stale test rerun 2/2 green; full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green.

## [Completed] BUI-BUILOG-LINT-001 — trace-phase lint wrapper coverage (2026-07-02)
**Summary**: Closed the review finding that `tools/lint/lint_log_messages.lua` did not scan every trace wrapper family now carrying AI-facing builog phase records. The phase linter now covers `TraceBankState`, `TraceVendorBootstrap` conditional phases, and `TraceNativeStoreBridge`, including the split-call conditional shape used by the Vendor native-store bridge. Existing non-canonical Banking/Vendor bridge phases surfaced by the new coverage are explicitly triaged through the approved-legacy WARN path so the monitor contract remains visible without forcing noisy phase churn.
**Source Issue/Phase**: BUI-BUILOG-LINT-001 (`project-improvements.md`)
**Related Files/Tests**: `tools/lint/lint_log_messages.lua`, `tools/tests/test_builog_flow_source.lua`, `docs/planning/project-improvements.md`. Validation: `test_validate(luac_syntax)` passed for touched Lua files, targeted `test_validate(lua_run, tools/tests/test_builog_flow_source.lua)` passed, and `test_validate(lua_run, tools/lint/lint_log_messages.lua)` passed against the current module tree.

## [Completed] BUI-DEEPDIVE-002 — implementation review remediation (2026-07-02)
**Summary**: Closed the follow-up review findings from commit `d1e4e5ac`. Vendor native-store takeover now removes the dead method-prehook installer and replaces the native `STORE_WINDOW_GAMEPAD.control` open/close callbacks with BetterUI-owned wrappers that suppress native handling only while BetterUI owns the flow, route disabled/non-owned opens and closes through public native fallback helpers in `VendorInteractionRuntime`, preserve specialized native scenes, and gate Vendor event handlers when the module is disabled. The native fallback open path now mirrors ESOUI's component list including Vengeance sell mode.

**ResourceOrbFrames fixes**: Cooldown visuals can be rearmed by `EVENT_ACTION_SLOT_ABILITY_USED` and `EVENT_ACTION_UPDATE_COOLDOWNS` after the idle latch has disarmed, so the next tick scans again. EventRegistry namespaces now use the `BETTERUI_ResourceOrbFrames` prefix as the actual `EVENT_MANAGER` namespace suffix source in `OrbEvents`, `OrbBars`, `OrbVisuals`, and the root module; the one-shot init unregister uses the matching tracked module key. `EVENT_PLAYER_ALIVE` again schedules native-bar hide enforcement.

**Quality cleanup**: Removed the duplicate secondary dialog ownership warning, fixed the inventory destroy refresh callback indentation, and removed the trailing whitespace in the guild bank dialog title block. The BUI-DEEPDIVE-001 completion note was corrected to distinguish the original partial namespace/cooldown/suppression work from this remediation pass.

**Source Issue/Phase**: BUI-DEEPDIVE-002 (`project-improvements.md`)
**Related Files/Tests**: `Modules/Vendor/Core/Bridge/VendorNativeStoreBridge.lua`, `Modules/Vendor/Core/Lifecycle/VendorInteractionRuntime.lua`, `Modules/Vendor/Vendor.lua`, `Modules/ResourceOrbFrames/{Core/OrbEvents.lua,Core/OrbBars.lua,Core/OrbVisuals.lua,ResourceOrbFrames.lua}`, `Modules/CIM/Dialogs/DialogRegistry.lua`, `Modules/Inventory/Dialogs/InventoryDialogs.lua`, `Modules/Banking/Core/GuildBankAdapter.lua`, `tools/tests/{test_orb_events.lua,test_resource_orbframes_core_support_source.lua,test_resource_orbframes.lua,test_vendor_native_store_rebuild_source.lua,test_vendor_interaction_runtime.lua,test_dialog_registry_source.lua}`, planning docs. Validation: focused `lua_run` for five remediation tests green; targeted `test_resource_orbframes.lua` green; `luac_syntax` 453/453 green; full `test_validate(lua_run, tools/tests/run_all_tests.lua)` green.

## [Completed] BUI-DEEPDIVE-001 Phases 1–5 — compatibility, hot-path, consolidation, lifecycle hygiene (2026-07-02)
**Summary**: Completed the full BUI-DEEPDIVE-001 drain. Cross-addon compatibility fixes removed permanent native-store unregisters and narrowed native-store takeover risk, added reversible dialog ownership tracking with `CIM.Dialogs.Restore()`, scene-gated stock inventory dialog replacements, removed the dead `BETTERUI.ReplaceHook` path, and required real `ZO_PostHook` semantics for GeneralInterface mail hooks. ResourceOrbFrames update-loop namespaces and registry tracking were moved toward the `BETTERUI_ResourceOrbFrames` prefix, its death handling was consolidated under one `EVENT_PLAYER_DEAD` registration, and the 16ms cooldown visual tick introduced an armed/disarmed latch with generation-aware button state reporting so idle ticks skip bar scans after setup/enable. BUI-DEEPDIVE-002 completed the actual EventRegistry namespace prefixing, replaced the attempted native-store suppression prehooks with control-event wrappers, and added event rearming for cooldowns that begin after the latch has gone idle.

**Performance and consolidation**: Hot trace wrappers now preflight `Log.EnabledFor()` before payload construction in GeneralInterface and ResourceOrbFrames paths, `Log.MakeTracer()` provides a shared tracer factory adopted by Banking, Companions runtime, and Vendor bootstrap, `CIM.Utils.GetCurrentSceneName()` centralizes listed scene-name accessors, `CIM.Utils.InstallNativeSceneRedirect()` owns Banking/Companions scene redirect wiring, and `CIM.Lists.WrapMovePreviousToHeader()` owns the shared header-entry MovePrevious wrapper. Dialog registration now routes through `BETTERUI.CIM.Dialogs.Register()` outside the registry; raw `ESO_Dialogs[` and `ZO_Dialogs_RegisterCustomDialog` access is confined to `Modules/CIM/Dialogs/DialogRegistry.lua`.

**Cleanup slice**: `CIM.Dialogs.ShouldTraceSliderPreview()` owns ITEM_SLIDER trace-bucket dedupe for banking and craft-bag quantity dialogs. Nameplates legacy string style migration moved out of `GetSettings()` into lifecycle entry points. Tooltip mouse-wheel enhancement now has `RestoreTooltipMouseWheel()` and cleanup restores native mouse state/handler. `test_control_utils.lua` is the canonical ControlUtils suite; `test_controlutils.lua` is a no-op compatibility shim.

**Source Issue/Phase**: BUI-DEEPDIVE-001 Phases 1–5 (`project-improvements.md`)
**Related Files/Tests**: Vendor native store bridge/runtime, Inventory dialog/action hooks, Banking guild/multiselect/quantity dialogs, Companions actions/dialogs/runtime, CIM `Log`, `Utilities`, `DialogRegistry`, `HookFactory`, `ListRefreshManager`, GeneralInterface setup/tooltips, Nameplates, ResourceOrbFrames event/bars/visuals/cooldown files, TradingHouse/Vendor dialog call sites, docs and source-contract tests. Validation: `test_validate(luac_syntax)` 453/453 green; `test_validate(lua_run, tools/tests/run_all_tests.lua)` green; targeted rerun of prior failures (`test_banking_guild_bank.lua`, `test_companions_scene_lifecycle.lua`, `test_control_utils.lua`, `test_orb_events.lua`, `test_trading_house_search_presets.lua`, `test_vendor_sell_all_junk_dialog.lua`) green.

## [Completed] BUI-TRACE-003 Phase 7 — review remediation follow-up (2026-07-02)
**Summary**: Closed the two host-review follow-up findings from BUI-TRACE-003. `tools/lint/lint_log_messages.lua` now covers the combat/HUD and GeneralInterface wrapper families that emit AI-facing Phase 3 records (`TraceUltimate`, `TraceCoordinator`, `TraceCastBar`, `TraceOrbEvents`, `TraceNameplates`, `TraceGeneralInterface`, `TraceGeneralSetting`, `TraceDrag`), with newly surfaced legacy phases explicitly triaged through the approved-legacy WARN path instead of silently bypassing canonical-phase lint. Direct builog disable paths now clear pending watchdog expectations via `InterfaceLog.SetLoggingEnabled(false, source)`, preventing stale expectations from firing misleading anomaly WARNs after re-enable while preserving the final disabled breadcrumb.
**Source Issue/Phase**: BUI-TRACE-003 Phase 7 (`project-improvements.md`)
**Related Files/Tests**: `tools/lint/lint_log_messages.lua`, `tools/tests/test_builog_flow_source.lua`, `Modules/CIM/Core/Diagnostics/InterfaceLog.lua`, `tools/tests/test_interface_log.lua`, `tools/tests/test_watchdog.lua`, `docs/planning/project-improvements.md`. Validation: phase lint exit 0, targeted watchdog/interface/log-flow tests green, full `run_all_tests.lua` green, `luac_syntax` green for touched Lua files, planning validation green after migration.

## [Completed] BUI-TRACE-003 Phases 1–6 — uniform input anchor, anomaly watchdog, combat/HUD parity, monitor digest, in-scope extractions (2026-07-02)
**Summary**: Implemented all six BUI-TRACE-003 phases. **Phase 1**: new `Modules/CIM/Keybinds/InputAnchor.lua` (143 lines; registered after Diagnostics, before all keybind factories) — `Wrap`/`WrapGroup` emit the standard `INFO KEYBIND | input.keybind fired` cause anchor (module/action/keybind/gamepad/enabled, TRACE-only `binding=`), set `lastAction`, never swallow callback errors, wrap idempotently, and never evaluate `name`/`visible` resolvers; adopted via a shared `anchor.WrapGroup`/`Wrap` pattern in Banking `KeybindManager`, `InventoryKeybinds` (CraftBag reuses the wrapped inventory group; no descriptors of its own), `VendorKeybinds`, `TradingHouseRuntime`, and NEW coverage in Companions (`CompanionListManager`, `CompanionsRuntime`, `CompanionDialogs`) and GeneralInterface `Setup.lua` (Writs has no keybind descriptors — covered by the existing list-trigger/dialog traces). Grep gate holds: `"fired"` appears only in InputAnchor. **Phase 2**: new `Modules/CIM/Core/Diagnostics/Watchdog.lua` (234 lines) — `Expect`/`Resolve` with compound kind+key, single self-stopping 1 s sweep (runs only while `Log.IsActive()` AND expectations pending), max-64 cap with `anomaly overflow` WARN, `anomaly detected` WARN with kind/key/ageMs/timeoutMs+context, `GetStats()` wired into `/buihealth` and `/builog status`; auto-wired to `Log.FlowBegin/FlowEnd` (30 s, gated on the flow-begin emit gate), Banking `bank.transfer` (resolve on cleared AND expired), TradingHouse `th.op` (30 s), `ListRefreshManager` `list.refresh` (5 s, per-manager keys, resolves on executed/stale/clean/cancelled/coalesced); `Log.ApplyPreset("off")` deactivates it. **Phase 3**: transition-only combat/HUD events with previous-state comparison — `resource_orbs.ultimate changed` (UltimateManager), `resource_orbs.bar_swap changed` (Coordinator), `resource_orbs.cast begin/end` (OrbBarUpdates), `resource_orbs.combat changed` (OrbEvents across combat-state/dead/alive/activated events), `nameplates.visibility changed` + `nameplates.refresh end`, GeneralInterface mail-delete lifecycle phases normalized to `requested`/`confirmed`/`skipped`; snapshot providers enriched (`resourceOrbs` adds `combat=`/`bar=`/`ultReady=`, `nameplates` adds `activeRules=`). **Phase 4**: `monitor.sh digest [--since|--last] [--jsonl]` — pure-awk flow-grouped timeline (cause `input.keybind` → key records → outcome or `UNRESOLVED`) with anomalies, WARN/ERROR, real Lua errors, drop summaries, screenshot markers, preamble, and session-report sections; `--jsonl` emits one object per record; `/builog report` emits the `STATE | session report` end-of-session anchor (sink/error/watchdog/anomaly/screenshot counters); SKILL.md rewritten digest-first. No new host dependencies (bash/awk/sed only). **Phase 5**: behavior-preserving extractions with load-order + delegating aliases — `DomainLog.lua` (236 lines; Log.lua 927→779 with `Log.DescribeItem`/etc. aliases + load-order assert), `BuilogCommands.lua` (496 lines; InterfaceLog.lua ~950→683, transport/budget/persistence only), `VendorKeybinds.lua` (546 lines; Vendor.lua ~2,000→1,540). **Phase 6**: lint `CANONICAL_PHASE` gained `fired`/`detected`/`overflow`/`report`; docs synced (`builog-developer-guide.md`, `logging-host-tail-parse.md`, `logging-playbook.md`, monitor SKILL, `changelog.txt`); BUI-TRACE-002 L4 checklist extended with keybind/anomaly/combat/digest items. Host code review (2026-07-02) verified all ACs and drove the digest end-to-end against a synthetic fixture (cause→outcome grouping, UNRESOLVED flagging, anomaly/real-error sections correct); two findings (lint `PHASE_PATTERNS` misses the pre-existing combat/HUD/GeneralInterface wrapper families now carrying AI-facing events; `/builog off` via `SetLoggingEnabled(false)` bypasses `Watchdog.Deactivate`) were remediated in **BUI-TRACE-003 Phase 7** above.
**Source Issue/Phase**: BUI-TRACE-003 Phases 1–6 (`project-improvements.md`)
**Related Files/Tests**: new `Modules/CIM/Keybinds/InputAnchor.lua`, `Modules/CIM/Core/Diagnostics/{Watchdog,DomainLog,BuilogCommands}.lua`, `Modules/Vendor/Core/VendorKeybinds.lua`; modified `Modules/CIM/Core/Diagnostics/{Log,InterfaceLog,WatchMode}.lua`, `Modules/CIM/Lists/ListRefreshManager.lua`, `Modules/Banking/{Actions/TransferActions,Keybinds/KeybindManager}.lua`, `Modules/Inventory/Keybinds/InventoryKeybinds.lua`, `Modules/TradingHouse/Core/{TradingHouseRuntime,TradingHouseRuntimeFlow}.lua`, `Modules/Vendor/Vendor.lua`, `Modules/Companions/{Core/CompanionListManager,Core/CompanionsRuntime,Dialogs/CompanionDialogs}.lua`, `Modules/GeneralInterface/Setup.lua`, `Modules/Nameplates/Nameplates.lua`, `Modules/ResourceOrbFrames/{Core/OrbBarUpdates,Core/OrbEvents,SkillBar/Coordinator,SkillBar/UltimateManager,ResourceOrbFrames}.lua`, `BetterUI.txt`, `tools/lint/lint_log_messages.lua`, `tools/builog-monitor/{monitor.sh,SKILL.md}`, docs; new tests `test_input_anchor.lua`, `test_watchdog.lua`, `test_resource_orbs_trace_source.lua`; extended `test_builog_flow_source.lua` (InputAnchor load order + adoption + no-bespoke-fired gates), `test_builog_monitor_source.lua` (digest fixture + JSONL), `test_interface_log.lua` (`/builog report` counters), `test_log.lua` (DomainLog aliases), `test_vendor_trace_source.lua`. Validation: full `run_all_tests.lua` green via `test_validate(lua_run)`, `luac_syntax` 452/452, phase lint exit 0 (336 approved-legacy WARNs, 0 non-canonical), fixture digest run green, `validate_planning.sh` green post-migration.

## [Completed] BUI-TRACE-002 Phase 8 (host gate) + Phase 10 — validation/docs gate and final review remediation (2026-07-02)
**Summary**: Closed out the BUI-TRACE-002 host-side gate. **Task 10.1**: `lint_log_messages.lua` `PHASE_PATTERNS` extended from 7 to 24 patterns covering every module trace wrapper introduced in Phases 2–7 (`TraceVendorEvent`, `TraceVendor`, `TraceVendorBatch`, `TraceVendorBootstrap`, `TraceBrowse`, `TraceSell`, `TraceListings`, `TraceTH`, `TraceTHFlow`, `TraceWrit`, `TraceWritEvent`, `TraceCompanionRuntime`, `TraceCompanionDialog`, incl. conditional-argument variants); newly surfaced phases triaged into `APPROVED_LEGACY_PHASE` (WARN-visible count grew 90 → 336 — pure visibility gain from covering previously-silent surfaces; 0 non-canonical, exit 0). **Task 10.2**: the unconditional 48-char `lastAction` bound is now a documented contract — stated in `logging-host-tail-parse.md` (both the privacy section and the context-suffix section) and locked in `test_watchmode.lua` for privacy ON and OFF (context suffix + heartbeat snapshot). **Task 8.1**: contract suite, new tests, full `run_all_tests.lua`, `luac_syntax` 444/444, and phase lint all green post-Phase-10; `monitor.sh` expectations updated for `TRANSFER` and the drop-summary reasons. **Task 8.2**: reference docs + `changelog.txt` synced (inspect preset, `/builog privacy`, `truncated=1`, `reason=priority_rate_limit`, expanded module landmark coverage). **Task 8.3 (host half)**: third host code review (2026-07-02) verified Phase 8 + 10 ACs with no new findings. The user-assisted in-game monitor checkpoint remains the sole open item (release-gate only) in `project-improvements.md`.
**Source Issue/Phase**: BUI-TRACE-002 Phases 8 (host gate) + 10 (`project-improvements.md`)
**Related Files/Tests**: `tools/lint/lint_log_messages.lua`, `tools/builog-monitor/{monitor.sh,SKILL.md}`, `docs/reference/{builog-developer-guide,logging-host-tail-parse,logging-playbook}.md`, `docs/publishing/changelog.txt`, `tools/tests/test_watchmode.lua`, `tools/tests/test_builog_monitor_source.lua`. Validation: full `run_all_tests.lua` green, `test_validate(luac_syntax)` 444/444, phase lint exit 0 (336 approved-legacy WARNs, 0 non-canonical), `validate_planning.sh` green.

## [Completed] BUI-TRACE-002 Phases 5–7 + 9 — transport hardening, privacy mode, module envelopes, review remediation (first-cut) (2026-07-01)
**Summary**: Implemented the remaining behavior phases of BUI-TRACE-002 plus the Phase 9 review remediation. **Phase 5**: `IsPriorityLine` now header-anchored (level/category parsed from the record header, payload text can no longer promote a line) with a `PRIORITY_CATEGORIES` table incl. `TRANSFER`; priority lines get a bounded 4× secondary frame/second cap and always respect `maxPending`, with per-reason drop summaries (`reason=priority_rate_limit` vs `rate_limit`); zero-clock pregame windows skip per-second accounting; `/builog capture` snapshots and restores `{preset, minLevel, payloadCapture, enabled}` via new `preservePreset` setter args; `RawEmit` caps lines at 2048 bytes with a UTF-8-safe prefix (`Utf8SafePrefix`) + ` truncated=1` marker. **Phase 6**: `/builog privacy on|off` with `Log.SetPrivacyMode`/`GetPrivacyMode`, persisted as `interfaceLogPrivacy` and restored in `RuntimeSetup.Apply`, surfaced in `/builog status`; when ON the preamble omits `player`/`zone` and addon names (count only), `DescribeItem` omits `name=`, and currency snapshots emit deltas only (Banking `CurrencyAmountForLog` + `AddCurrencySnapshotFields`, Vendor `ScrubVendorPrivacy`); `lastAction` bounded to 48 chars in context suffix + snapshots; docs updated (dev guide, playbook, host-tail-parse, monitor SKILL). **Phase 7**: `vendor.scene` begin/end (interaction + bootstrap runtimes), `vendor.mode` requested→changed→applied chain (`VendorControllerRuntime` + `VendorModePolicy`), user-fired `vendor.keybind` traces with flow (never label getters), `th.mode changed` + aggregate `th.list end` across Browse/Sell/Listings, `writs.state changed` at station open/refresh/craft/close; new source-contract tests `test_vendor_trace_source.lua`, `test_writs_trace_source.lua`. **Phase 9 remediation (all six 2026-07-01 review findings)**: stale `test_cim_list_support_source.lua` signatures and stale `test_builog_flow_source.lua` IsPriorityLine assertions re-targeted (suite green again); `ListRefreshManager` keeps the latest NON-NIL flow/source/reason/token across flowless coalesced queues; `Vendor.ScheduleActionSettled` early-returns when logging is inactive (no timer scheduled for non-logging players); `requested` now precedes `refresh_decision` in `ExecuteDirectTransfer`; UTF-8-safe truncation. Host review (2026-07-01, second pass) verified all ACs; two residual findings (lint `PHASE_PATTERNS` misses the new module trace wrappers; unconditional 48-char lastAction bound needs a documented contract) tracked as **BUI-TRACE-002 Phase 10**; Phase 8 gate (incl. user-assisted in-game monitor checkpoint) remains open.
**Source Issue/Phase**: BUI-TRACE-002 Phases 5, 6, 7, 9 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/{Log,InterfaceLog,WatchMode}.lua`, `Modules/CIM/Core/Lifecycle/RuntimeSetup.lua`, `Modules/CIM/Lists/ListRefreshManager.lua`, `Modules/Banking/Keybinds/KeybindManager.lua`, `Modules/Banking/Actions/TransferActions.lua`, `Modules/Vendor/Vendor.lua` + `Core/Lifecycle/{VendorInteractionRuntime,VendorControllerRuntime}.lua` + `Core/Policy/VendorModePolicy.lua` + `Core/{VendorBootstrapRuntime,VendorClass}.lua`, `Modules/TradingHouse/Core/TradingHouseClass.lua` + `Components/*`, `Modules/Writs/{Core/Writ,Module}.lua`, docs `builog-developer-guide.md`/`logging-host-tail-parse.md`/`logging-playbook.md`/`tools/builog-monitor/SKILL.md`; tests `test_interface_log.lua`, `test_log.lua`, `test_watchmode.lua`, `test_runtime_setup_apply.lua`, `test_log_parse_contract.lua`, new `test_vendor_trace_source.lua` + `test_writs_trace_source.lua`, fixed `test_cim_list_support_source.lua` + `test_builog_flow_source.lua`. Validation: full `run_all_tests.lua` green, `test_validate(luac_syntax)` 444/444, phase lint exit 0 (90 approved-legacy WARNs), `validate_planning.sh` green.

## [Completed] BUI-TRACE-002 Phases 1–4 — taxonomy, outcome confirmation, correlation/context (first-cut) (2026-07-01)
**Summary**: Implemented the first four phases of BUI-TRACE-002 (~2.1k added lines across 50 files). **Phase 1**: `Log.EVENT_SCHEMA` versioning wired into `traceVersion` + the watch preamble; `TRANSFER` category split out of `ACTION` for item/currency transfers (dialog lifecycle retained under `ACTION`) with `IsPriorityLine`, monitor, host-parse doc, and parse-fixture updates; canonical phase vocabulary migrated in the Phase-1 target files with `lint_log_messages.lua` enforcing canonical tokens plus a visible approved-legacy WARN list (90 legacy phases remain, 0 non-canonical). **Phase 2**: `bank.item_transfer confirmed` (INFO) on `EVENT_INVENTORY_SINGLE_SLOT_UPDATE`/guild-bank slot events and `expired` (WARN) on timeout, both carrying `flow` + `pendingRemaining` via flow-bearing `_pendingTransfers` entries; Vendor components emit `requested`/`settled` with `goldBefore`/`goldAfter`/`goldDelta` through shared `Vendor.TraceActionRequested`/`ScheduleActionSettled` helpers. **Phase 3**: TradingHouse pending-operation registry correlates `requested`→`trading_house.response completed|failed` by `opId` across response, error, response-timeout, operation-timeout, and dialog-cancel paths; vendor batch lifecycle emits `begin`/`step`/`end`/`abort` with `batchId`. **Phase 4**: `ListRefreshManager` `QueueRefresh`/`ExecuteRefresh`/`SavePosition`/`RestorePosition` accept trace options (`flow`/`source`/`reason`/`token`/`coalescedCount`) propagated from `RefreshBankListAfterTransfer` across the task-scheduler boundary; WatchMode hardcoded view↔scene mapping replaced by `Watch.RegisterViewScene` (multi-scene capable) with view registration + `SetView` coverage across Banking, Inventory, Vendor, TradingHouse, Companions, and Writs. Host code review (2026-07-01) verified the acceptance criteria; six review findings (two stale pre-existing source-contract tests leaving the suite red, flowless-coalesce flow erasure, ungated vendor settled-timer, direct-transfer record ordering, UTF-8 truncation boundary) are tracked as **BUI-TRACE-002 Phase 9** in `project-improvements.md`; final acceptance occurs at the Phase 8 gate.
**Source Issue/Phase**: BUI-TRACE-002 Phases 1–4 (`project-improvements.md`)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/{Log,InterfaceLog,WatchMode}.lua`, `Modules/CIM/Lists/ListRefreshManager.lua`, `Modules/CIM/Keybinds/GenericKeybinds.lua`, `Modules/Banking/Actions/{TransferActions,BankingActions}.lua`, `Modules/Banking/Keybinds/KeybindManager.lua`, `Modules/Banking/{Banking,State/StateManager}.lua`, `Modules/Vendor/Vendor.lua` + `Components/*` + `Core/VendorBatchRuntime.lua` + `Core/Lifecycle/VendorControllerRuntime.lua`, `Modules/TradingHouse/{TradingHouse,Core/TradingHouseClass,Core/TradingHouseRuntimeFlow}.lua` + `Components/*`, `Modules/Companions/{Module,Core/CompanionsRuntime}.lua`, `Modules/Writs/Core/Writ.lua`, `Modules/Inventory/{Inventory,State/ListStateManager}.lua`, `tools/lint/lint_log_messages.lua`, `tools/builog-monitor/{monitor.sh,SKILL.md}`, docs `builog-developer-guide.md`/`logging-host-tail-parse.md`/`logging-playbook.md`; new tests `test_vendor_action_outcomes.lua`, `test_trading_house_trace_source.lua`, `test_batch_lifecycle_source.lua`, `test_list_refresh_manager.lua` (all passing); validation `test_validate(luac_syntax)` 442/442, targeted `lua_run` suites green, full `run_all_tests.lua` pending the two Phase 9 stale-test fixes.

## [Completed] Addon Compatibility Hardening — Multi-Pass Cooperative Hook + Lifecycle Fixes (2026-06-26)
**Summary**: Per user request, ran the 2026-06-26 multi-loop compatibility hardening session across the addon. The pass eliminated direct monkey-patching by replacing it with `ZO_PostHook` and `_G` cooperative chains, fixed `SetHandler` clobbering with `ZO_PostHookHandler` plus `_betteruiXxx` guards, and stopped `RegisterForEvent`/`RegisterCallback` accumulation with unregister-before-register patterns. Scene callbacks now use one-time flags, `zo_callLater` callbacks are guarded by module-enabled and scene-active checks, batch pipelines use token guards, `DeferredTask:CancelAll()` iterates safely, and `BatchOverlay` tracks delayed `zo_callLater` handles. `ActionDialogHooks` gained a self-reinstallation guard, `TradingHouseRuntime` guards scene fragment adds with `HasFragment`, `SceneCleanup` cancels multi-alias `RefreshManager` references, `SettingsFactory` deduplicates LAM panel registration through `_registeredPanels`, and `EquipAction` now uses an EventRegistry-aligned retry event. Total scope: 39 files changed, 846 additions, 174 deletions.
**Source Issue/Phase**: User request — 2026-06-26 multi-loop compatibility audit using delegated kimi + codex agents; no version bump / no tag per request.
**Related Files/Tests**: Banking, CIM, Companions, GeneralInterface, Inventory, ResourceOrbFrames, TradingHouse, Vendor, Writs. Validation: host-owned `luac_syntax` on all touched files.

## [Completed] Builog Inventory/Banking Flow Observability Hardening (2026-06-24)
**Summary**: Per user request, ran the improvement-cycle pass three times over the builog feature with delegated discovery/review from kimi, gpt-5.5/gpt-5.3-codex-spark, and Antigravity Gemini 3.5 Flash, then host-adjudicated and implemented the accepted findings. The live `watch`/`inspect` stream now surfaces compact, AI-readable inventory and banking cause/effect landmarks for primary-action resolution/invocation, dialog action confirmation and restore outcomes, keybind-group refresh summaries, coalesced inventory refreshes with `updates=`, bank list refreshes, blocked item transfers with reasons, and currency transfers that complete or fail. `InterfaceLog` now enforces `maxPending` and drops stale off/on callbacks by generation; `SafeExecute` now preserves a source boundary for non-string errors such as `error(table)`; hot-path inventory update logging is coalesced so observability does not become list-update spam.
**Source Issue/Phase**: User request — critical builog/AI-monitoring robustness pass for real-time play-test diagnosis; three delegated review/fix loops plus host validation; no version bump / no tag requested.
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/InterfaceLog.lua`, `SafeExecute.lua`, Inventory action/state/list files, Banking action/keybind/state files, docs/reference logging docs, `tools/builog-monitor/SKILL.md`, `docs/publishing/changelog.txt`; tests `test_interface_log.lua`, `test_safe_execute.lua`, `test_builog_flow_source.lua`, `test_banking_keybinds.lua`. Validated via `test_validate(luac_syntax)` on touched Lua files, targeted `test_validate(lua_run)` builog/banking/inventory suites, full `test_validate(lua_run, tools/tests/run_all_tests.lua)`, markdown `text_contract` checks, `validate_manifest.sh`, `validate_types.sh`, `validate_planning.sh`, `run_syntax_check.sh`, and `lint_log_messages.lua`.

## [Completed] Feature-Requests Backlog Drain to Zero — 7 Implemented (First-Cut) + 3 Discarded (2026-06-19, cycle 3)
**Summary**: Per user direction (drain all planning files to zero; for anything needing in-game iteration, leverage the ESOUI reference, implement what's possible host-side, and close the ticket for the maintainer to test/fix in-game; gpt-5.5 reviews all implementations), drained the entire feature-requests backlog. **Disposition gate** (gpt-5.5 + kimi cross-model + host): 7 IMPLEMENT, 3 DISCARD. Delegation: kimi implemented the module-scoped features (3 parallel workers on non-overlapping modules), host implemented the CIM features, gpt-5.5 reviewed everything. Full `run_all_tests.lua` suite green; `luac_syntax` 413/413; manifest validated.
**Implemented** (first-cut; in-game validation is the remaining manual checkpoint — see feature-requests "In-Game Validation Checkpoints"):
- **PLT-003** (host) — `GenericWindow` category-position persistence now delegates to the uniqueId-aware `BETTERUI.CIM.PositionManager` under a per-window namespace, so Banking/Backpack restore onto the same ITEM (not just index) after a reorder/refresh. Self-contained (only `SwitchToCategory` calls it); failure mode bounded to a wrong index. +`test_generic_window_position_manager.lua`.
- **PLT-006** (host) — narration-broadening capability: new `NarrateActionKeybinds` builder + optional `providers` arg to `RegisterListNarration` (getCategory/getCurrency/getMode/getKeybinds), each pcall-isolated + backward-compatible. +`test_narration_providers.lua`. Per-scene wiring is the in-game (screen-reader) iteration.
- **INV-003** (kimi) — companion equipment item-preview action via `CanInventoryItemBePreviewed`/`PreviewInventoryItem`/`EndCurrentItemPreview`, with scene-exit teardown. +`test_companion_item_preview.lua`.
- **TRC-002** (kimi) — pure, exported, tested `Vendor.ClampPurchaseQuantity` (stack/affordability clamp) as scaffolding for a future quantity spinner; the buy primary action stays single-item (host reverted the worker's buy-whole-stack default, which broke the buy-1 contract). +`test_vendor_buy_quantity.lua`.
- **TRC-005** (kimi) — active-mount icon via `GetActiveCollectibleByType(COLLECTIBLE_CATEGORY_TYPE_MOUNT, GAMEPLAY_ACTOR_CATEGORY_PLAYER)`+`GetCollectibleIcon` (replaces the generic icon) + a no-mount-skin warning. +`test_vendor_stable_active_mount.lua`.
- **TRC-003** (kimi) — guild-store browse filter-editing (name/category/price/quality/level) via new `BrowseFilters.lua` (pure helpers + native-feature wiring + filter dialog) and an Edit-Filters keybind. **Host fix (gpt-5.5 must-fix)**: `BrowseComponent:ExecuteSearch` now defers a fresh search to native `DoSearchWhenReady()` when a name match is pending, so the name filter applies on the first search instead of being dropped. +`test_trading_house_browse_filters.lua`.
- **TRC-004** (kimi) — create-listing digit price entry via new `PriceEntry.lua` (`ZO_CurrencySelector_Gamepad` + pure `ClampListingPrice`) for exact large-range prices. +`test_trading_house_price_entry.lua`. New Core files registered in `BetterUI.txt` (load order verified: before `BrowseComponent`).
**Discarded** (recorded reasoning; reopen on concrete need):
- **INV-002** — slot-centric companion category model (native parity): BetterUI deliberately uses inventory-style filter categories; implementing native parity would reverse an intentional design decision (a product call, not an iteration-blocked gap), low marginal value, high blind-rebuild risk.
- **PLT-002** — non-EN locale backfill (~83 EN-only ids ×6 locales): no host-implementable code — needs human translators; EN fallback works at runtime; machine translation risks unfit ESO-specific terminology. Reopen when translations are contributed (preview-first `l10n_*` flow).
- **PLT-004** — migrate remaining CIM duplication clusters: pure internal refactor, no user-facing feature, high cross-module blind-regression risk, each cluster needs in-game validation (akin to the discarded PLT-001 blind-split). Reopen as focused per-cluster defects only if a real bug appears.
**Review/host-adjudication**: gpt-5.5 reviewed all 7 against the ESOUI source — 6 SOUND, 1 must-fix (TRC-003 async name-match gating, fixed above). Host caught + fixed 2 delegate-introduced regressions before acceptance: a Companions test missing `WEAPONTYPE_*` load stubs, and the Vendor buy-whole-stack default breaking the buy-1 contract test.
**Source Issue/Phase**: 2026-06-19 improvement-cycle (cycle 3) — feature-requests backlog drain to zero; kimi (module impl) + host (CIM impl) + gpt-5.5 (review); no version bump / no tag per request.
**Related Files/Tests**: `Modules/CIM/Core/Window/GenericWindow.lua`, `Modules/CIM/Core/Integration/NarrationHelper.lua`, `Modules/Companions/Actions/CompanionActions.lua` + `Core/CompanionsRuntime.lua`, `Modules/Vendor/Components/BuyComponent.lua` + `StableTrainingComponent.lua`, `Modules/TradingHouse/Core/BrowseFilters.lua` (new) + `PriceEntry.lua` (new) + `Components/BrowseComponent.lua` + `Core/TradingHouseRuntime.lua` + `Core/TradingHouseRuntimeFlow.lua`, `BetterUI.txt`; new tests `test_generic_window_position_manager.lua`, `test_narration_providers.lua`, `test_companion_item_preview.lua`, `test_vendor_buy_quantity.lua`, `test_vendor_stable_active_mount.lua`, `test_trading_house_browse_filters.lua`, `test_trading_house_price_entry.lua`. Validated via `test_validate(luac_syntax)` (413/413) + full `test_validate(lua_run, run_all_tests.lua)` (green) + `tools/tests/validate_planning.sh` (open-items-only, green).

## [Completed] HUD-005 Radial Cooldown Fix + PLT-006 Hardening Archive + Worktree Purge (2026-06-19, cycle 2 follow-up)
**Summary**: Follow-up to the cycle-2 re-validation (entry below), per user direction. **HUD-005 implemented** (it had been recorded Blocked L4): `Modules/ResourceOrbFrames/SkillBar/FrontBarCooldowns.lua` `StartCooldownIfChanged` now restarts the non-gamepad radial when a NEW cooldown window begins — a duration change OR a same-duration refresh where the remaining time jumps back up >100ms vs the previous tick — via a new pure predicate `ShouldRestartRadialCooldown`; `lastSeenCooldownRemainMs` is latched every tick and reset alongside `appliedCooldownDurationMs` in both the gamepad-hide and cooldown-ended branches. This fixes a stale radial wipe on proc/early-recast refreshes (e.g. `GetActionSlotEffectTimeRemaining` buffs/DoTs) while leaving normal per-frame countdown untouched (the +100ms margin prevents restart churn). +`tools/tests/test_front_bar_cooldown_restart.lua` (predicate edge cases incl. the +100ms boundary, plus a stateful frame-sequence test). Reviewed SOUND by an independent kimi adversarial diff pass (host + kimi + codex concur); full `run_all_tests.lua` suite green. **In-game keyboard-mode radial/text alignment smoke is the remaining manual checkpoint.** **PLT-006 split**: the narration **hardening** half — `canNarrate`/`selectedNarrationFunction` callback bodies pcall-wrapped + `test_narration_callbacks.lua` — is implemented and archived here (in-game screen-reader smoke pending); the remaining **coverage broadening** (footer currency / mode / category / keybind labels) stays open in `feature-requests.md` as Blocked (L4). **Hygiene**: purged the stale orchestrator worktree `zff/wt/kimi/3b45418b` (unmerged, 193 files divergent, was `c52ed655`) per request — `git worktree remove` + `git branch -D`.
**Source Issue/Phase**: 2026-06-19 improvement-cycle (cycle 2 follow-up) — HUD-005 implementation, PLT-006 hardening archive/split, kimi worktree purge; no version bump / no tag per request.
**Related Files/Tests**: `Modules/ResourceOrbFrames/SkillBar/FrontBarCooldowns.lua` (predicate + restart latch + exports); new `tools/tests/test_front_bar_cooldown_restart.lua`; planning `docs/planning/feature-requests.md` (HUD-005 row removed, PLT-006 rescoped to broadening). Validated via `test_validate(luac_syntax)` + targeted `test_validate(lua_run, test_front_bar_cooldown_restart.lua)` + full `test_validate(lua_run, run_all_tests.lua)` (green) + `tools/tests/validate_planning.sh` (open-items-only, green).

## [Completed] Improvement-Cycle Re-Validation Drain — Cross-Model Gate (2026-06-19, cycle 2)
**Summary**: Re-ran `/improvement-cycle` (kimi for economy/standard delegations; codex for cross-model diversity + adversarial verification; **no version bump / no tag**, per request). **Multi-source validation gate on all 10 open feature-requests items** (INV-002/003, TRC-002/003/004/005, PLT-002/003/004/006): kimi + codex (cross-model) + host adjudication = 3-source consensus → **all KEEP-BLOCKED**; no safe code-only slivers and no scraps. Each remains valid but genuinely gated on in-game (L4) interactive validation or external translation. codex independently corroborated PLT-002 by measurement (`en.lua` 660 `SI_BETTERUI_*` ids vs 577 in each non-EN locale = 83 EN-only). **Static health**: `test_validate(luac_syntax)` 403/403 clean; full `run_all_tests.lua` suite green; API 101050 confirmed current (Update 50, per the v3.07 changelog); `LibAddonMenu-2.0>=41` floor + `OptionalDependsOn` are external/soft (no host-actionable upgrades). **Fresh engineer-review** of the prior cycle's 5 fixes (commits `d0193ea0..HEAD`) + their neighborhood: 4 confirmed regression-free; **1 new finding** surfaced → recorded as **HUD-005** (the front-bar radial cooldown stays at a stale wipe position on a same-duration cooldown/effect refresh; keyboard-only). HUD-005 was validated REAL by kimi + codex + host, but codex (adversarial verify) flagged that the fix changes visible radial behavior and may cause small restarts on legitimate upward timing corrections → **recorded Blocked (L4) with a concrete frame-delta fix sketch rather than force-shipped** (host cannot run ESO). **Manual checkpoints for the user** (outside host reach): (1) in-game L4 iteration pass for the 10 blocked items + HUD-005; (2) human translation for PLT-002 (~83 ids per non-EN locale); (3) stale orchestrator worktree `zff/wt/kimi/3b45418b` (193 files divergent from `develop`, HEAD `c52ed655`) is **unmerged** — left intact per the never-force-remove-unmerged-work rule; `git worktree remove` + `git branch -D` to prune if no longer needed.
**Source Issue/Phase**: 2026-06-19 improvement-cycle (cycle 2) — feature-requests re-validation drain + fresh engineer-review; no source code shipped (HUD-005 recorded for L4); no version bump / no tag per request.
**Related Files/Tests**: `docs/planning/feature-requests.md` (HUD-005 added under HUD and Frames); no source files changed. Validated via `test_validate(luac_syntax)` (403/403) + `test_validate(lua_run, tools/tests/run_all_tests.lua)` (full suite green) + `tools/tests/validate_planning.sh` (open-items-only, green).

## [Completed] Improvement-Cycle Review Remediation — Multi-Persona + Cross-Model Validation (2026-06-19)
**Summary**: Ran a fresh improvement cycle — 4 partitioned engineer-review slices (kimi: CIM diagnostics/lifecycle/integration, header-sort/nav/tooltip/settings; codex: ResourceOrbFrames per-frame HUD, batching/multiselect/Banking), each cross-model-verified by an independent adversarial verifier (codex verifies kimi's, kimi verifies codex's), then host adjudication. **29 findings surfaced; 24 implemented, 5 rejected/reverted.** Lua syntax 403/403 clean throughout; targeted per-area test suites green. **Implemented fixes** (in-game L4 smoke is the remaining manual checkpoint for interactive surfaces): PB-016 — Banking holds its `ListRefreshManager` at module scope (`BETTERUI.Banking.RefreshManager`) so the in-scope `SceneCleanup` step never reached it; `BankingSceneLifecycle:OnSceneHidden` now calls `RefreshManager:Cancel()` and exits multi-select, so an in-flight coalesced refresh can't fire post-teardown. **CIM diagnostics/integration**: optional market-addon (MasterMerchant/ATT/TTC) and AutoCategory `pcall` failures now log via `Log.Error` instead of silently degrading; `MarketIntegration` guards its resolved settings accessor; `GetPriorityOrderLive` returns a clone instead of the mutable internal `PRIORITY_ORDERS` constant. **CIM scene lifecycle**: the `StateChange` handler's previously-unprotected infrastructure calls (keybind add/remove, `taskManager:CancelAll`, `EventRegistry.UnregisterAll`) are now `SafeExecute`-wrapped and the `keybindsResolver` is `pcall`-protected, so one throw can no longer abort the rest of show/hide teardown; `Register` guards `scene.RegisterCallback`. **Header sort/nav/settings**: owner/sort/navigation callbacks (`onSortChanged`, `onControllerCreated`, `onEnter/onExitHeaderMode`, `onSave`, `onApply`) are `SafeExecute`-wrapped so a throwing owner callback can't strand keybind/tab-bar/nav state; nil-guards on `HeaderSortController` `baseName`/`callbackColumn` and `HeaderNavigation` `getCurrentIndex`; `GetModuleSettings` enforces a table type before cloning. **Tooltips**: toggling enhancements off now restores stock body-label fonts on the current tooltip immediately (was enlarged until a fresh tooltip rebuilt); `TooltipEquipped` guards `GetTooltipFontSize` when GeneralInterface is disabled. **ResourceOrbFrames per-frame latching** (extends the MPR-4 latch pattern): CastBar countdown text + fill style, mounted-stamina label/value, linear-cooldown static edge/overlay state, front-bar cooldown/quickslot/stack text+hidden, and ultimate child-lookups/cost/frame are now latched and skip redundant control setters on unchanged state. **Batching**: `MultiSelectManager:RefreshSelections` now preserves the `expectedSlotIdentity` stamp across a list refresh (without it, the kept lock/junk/destroy identity re-validation silently no-ops after a refresh); `ProtectionPolicy.CanDestroyItem` now **fails closed** (`DENY.NO_DESTROY_PROBE`) when the engine probe `ZO_InventorySlot_CanDestroyItem` is unavailable, matching its "probe is required" contract (+new fail-closed test block in `test_destroy_policy_contracts.lua`); the delayed Banking select-all callback rechecks scene visibility/selection mode before refreshing. **Rejected/reverted (recorded reasoning)**: S1-1 UserNotify→`ZO_Alert` "passes the raw id" — REVERTED as a false positive (ZO_Alert resolves string ids internally via `zo_strformat`; UserNotify intentionally passes the id and resolves only for the debug log; consistent with the MPR-2 refutation of the same claim); ClearSearchState `KEYBIND_STRIP` guard already present; NarrationHelper "missing unregister" — `ZO_ScreenNarrationManager` exposes no `UnregisterCustomObject`, registration is keyed by scene name (idempotent overwrite, no unbounded leak) and `canNarrate` gates by active scene; InterfaceLog drop-summary "popup" — uses the same deferred-error write path + global popup suppression as every log line, not a distinct defect; Banking transfer `IsSlotIdentityCurrent` re-validation — REVERTED as a re-litigation of the previously-discarded **PLT-005** (`HasItemAtSlot` is sufficient for non-destructive moves).
**Source Issue/Phase**: 2026-06-19 improvement-cycle — multi-persona engineer-review + cross-model adversarial validation gate + host adjudication against planning history (no version bump / no tag this cycle, per request)
**Related Files/Tests**: CIM (`Core/Diagnostics/SafeExecute.lua` [reverted no-op], `Core/Integration/MarketIntegration.lua` + `AutoCategoryIntegration.lua`, `Core/Lifecycle/SceneLifecycleManager.lua`, `Core/Settings/SettingsAccessor.lua`, `UI/HeaderSortController.lua` + `HeaderSortIntegration.lua` + `HeaderNavigation.lua`, `Core/Batching/MultiSelectManager.lua`, `Actions/ProtectionPolicy.lua`); GeneralInterface (`Tooltips/Tooltips.lua`); Inventory (`UI/TooltipEquipped.lua`); ResourceOrbFrames (`Core/OrbBarUpdates.lua`, `SkillBar/CooldownUtils.lua` + `FrontBarCooldowns.lua` + `UltimateManager.lua`); Banking (`Scene/BankingSceneLifecycle.lua`, `Core/MultiSelectActions.lua`); tests (`test_destroy_policy_contracts.lua` new fail-closed block; `test_header_sort_integration.lua` + `test_header_navigation_coalesce.lua` SafeExecute stub). Validated via `test_validate(luac_syntax)` (403/403) + targeted `test_validate(lua_run)` per changed area + the final full-suite gate.

## [Completed] Improvement-Cycle Drain + Planning Reconciliation (2026-06-19)
**Summary**: Drained the 2026-06-19 improvement-cycle review backlog and reconciled the planning docs to open-items-only (no completed/discarded rows left in the open files). **Priority-backlog resolved (each independently re-validated, fixed with unit tests; full Lua suite green, 179 files)**: PB-008 window core-keybind group now registered via a resolver closure instead of a by-value capture taken before the group is created (`WindowClass.lua`); PB-009 `SceneLifecycleManager` `StateChange` handler gained an unregister seam + double-registration guard (was stacking/leaking handlers); PB-010 clear-sort keybind gates on whether a sort is active rather than the cursor's current column (`HeaderSortKeybinds.lua`); PB-011 `ProtectionPolicy.CanDestroyItem` fails closed on a nil `slotType` (item-loss guard); PB-012 mail delete-confirmation bypass hooks the live `MAIL_INBOX_GAMEPAD` singleton post-load (was a no-op PreHook on a pre-load descriptor); PB-013 orb texture-coord/rotation reset runs when the animation toggle is turned off (`OrbEvents.lua`/`OrbVisuals.lua`); PB-014 CIM lifecycle/cleanup hardening cluster — (a) `GetSetting` no longer returns live table refs that bypass the change callback, (b) `SortManager.SortItems` decorate loop nil-guarded, (c) `HeaderNavigation` coalesced `zo_callLater` handle stored/cancelled, (d) `ListRefreshManager` pending-refresh `Cancel()` wired into scene cleanup, (e) dangling `headerSortKeybindDescriptor` cleared; PB-015 equipped/banked status label line-count/padding bounded against low-resolution overflow. Behavioral rows still want an in-game (L4) smoke per their acceptance notes. PB-001 (2026-06-10) closed as not-a-defect: the aggregate Lua harness runs end-to-end via `test_validate(action="lua_run", workDir=<repo root>, files=["tools/tests/run_all_tests.lua"])`; the earlier failure was running with `workDir=tools/tests`, which breaks the tests' repo-root-relative `dofile("Modules/...")` paths. **Feature-requests completed**: INV-001 consolidated both InventoryMultiSelect batch dialogs through `BETTERUI.CIM.Dialogs.Register` via a shared `BuildBatchDialogTemplate` (`ESO_Dialogs` no longer written directly; +`test_inventory_multiselect_dialogs.lua`); HUD-004 `CooldownUtils.ResolveCooldownWindow` reads the `isGlobalCooldown` (3rd) return of `GetSlotCooldownInfo` and suppresses only the GCD, matching `FrontBarManager` (+`test_cooldown_utils.lua`); INV-000 (Stack All keybind exposure, closed 2026-02-08) folded in here. **Discarded — reviewed, won't do (reopen only on concrete need)**: TRC-006 browse/purchase guild-specific store items (no `GetGuildSpecificItemInfo`/`BuyGuildSpecificItem` path; niche scope gap); PLT-001 split >600-line files by concern (VendorClass ~1840, Vendor ~1390, BankingClass ~1067, … are cohesive; a blind split is high-regression and net-negative — supersedes the MPR-2 deferral); PLT-005 re-validate transfer batches with `IsSlotIdentityCurrent` (`HasItemAtSlot` is sufficient for non-destructive moves); PLT-007 batch-coalescing mode for verbose logging (the per-frame/second InterfaceLog budget already bounds the frame-hitch risk).
**Source Issue/Phase**: 2026-06-19 improvement-cycle drain + planning-doc open-items-only reconciliation (PB-001, PB-008–PB-015, INV-001, HUD-004, INV-000; discards TRC-006/PLT-001/PLT-005/PLT-007)
**Related Files/Tests**: `Modules/CIM/Core/Window/WindowClass.lua`, `Modules/CIM/Core/Lifecycle/SceneLifecycleManager.lua`, `Modules/CIM/UI/HeaderSortKeybinds.lua`, `Modules/CIM/Actions/ProtectionPolicy.lua`, `Modules/GeneralInterface` (mail singleton hook), `Modules/ResourceOrbFrames/Core/OrbEvents.lua` + `Core/OrbVisuals.lua`, CIM cleanup cluster (`SortManager`/`HeaderNavigation`/`ListRefreshManager`/scene cleanup), `Modules/Inventory` + `Modules/Banking` status label, `Modules/CIM/Dialogs` (INV-001), `CooldownUtils` (HUD-004); tests incl. `test_cooldown_utils.lua`, `test_inventory_multiselect_dialogs.lua`, `test_narration_callbacks.lua`. Validated via `test_validate(lua_run, tools/tests/run_all_tests.lua)` — full suite green (179 files).

## [Completed] Real-Time Diagnostics Logging System + Addon-Wide Instrumentation (2026-06-17)
**Summary**: Built a unified, retail-safe diagnostics logging system that streams BetterUI debug output + caught Lua errors to the game's `live/Logs/interface.log` in real time (so an external tool/AI can tail it while playing), then instrumented the addon end-to-end. **Foundation**: new `BETTERUI.Log` facade (`Modules/CIM/Core/Diagnostics/Log.lua`) with levels (TRACE<DEBUG<INFO<WARN<ERROR), categories (SCENE/LIST/NAV/KEYBIND/FOOTER/CATEGORY/SEARCH/SORT/BATCH/ACTION/LIFECYCLE/SAFE/SETTINGS/GENERAL), per-level file/chat sinks, and `Log.Summarize`/`Log.IsActive`; new `InterfaceLog.lua` file sink that writes arbitrary lines by raising a deferred, popup-suppressed Lua error (retail has no `WriteToInterfaceLog`); `/builog on|off|test|chat on|off|popups on|off|level <lvl>` command. Inert until enabled, so normal players pay nothing; every call site nil-guarded (`if BETTERUI.Log then ...`), hot paths gated on `IsActive()`. `BETTERUI.Debug`/`DebugError`/`CIM.Debug.Log` are back-compat wrappers; `SafeExecute` caught errors route to `Log.Error("SAFE", …)`. **Instrumentation**: addon-wide gated logging across CIM (Lifecycle/Window/Batching/Keybinds/Actions/Lists/UI/Data), Inventory, Banking, Vendor, TradingHouse, Companions, ResourceOrbFrames, GeneralInterface, Nameplates, Writs — scene/list/keybind/sort/nav transitions, batch pipelines, nil-from-API guards, raw engine hook installs, init/event registration, plus central LAM `setFunc`/panel-registration logging in `SettingsFactory.RegisterModulePanel` (one change instruments ~104 settings). **Verified bug fixes** (adversarial verification; ~half of flagged candidates refuted as false positives): InventoryList `BETTERUI.Utils` nil-guard; 6 CIM wrapper-data/nil-guard hardening fixes; base-window `mainKeybindStripDescriptor` nil-deref guard; HeaderSortKeybinds clear-sort readiness gate. Confirmed-not-bugs (left unchanged): GenericKeybinds empty-check logic, TradingHouse `GetString(rawget…)` (constants registered), GenericHeader tabBar double-commit, UnifiedScreen footer load-order, MultiSelectManager activeInstance, ProtectionPolicy CanUnjunkItem.
**Source Issue/Phase**: User request — real-time Interface.log debug/error logging + comprehensive instrumentation + verified bug-fix pass
**Related Files/Tests**: new `Modules/CIM/Core/Diagnostics/Log.lua` + `InterfaceLog.lua` (+ `BetterUI.txt` manifest); `Modules/CIM/Core/Settings/SettingsFactory.lua` (central setFunc/panel instrumentation); `Modules/CIM/Core/Window/WindowClass.lua` + `Modules/CIM/UI/HeaderSortKeybinds.lua` (bug fixes); addon-wide instrumentation across CIM/Inventory/Banking/Vendor/TradingHouse/Companions/ResourceOrbFrames/GeneralInterface/Nameplates/Writs; docs (`Modules/CIM/Core/Diagnostics/README.md`, `reference/architecture.md`, `reference/tribal-knowledge.md`, `guides/testing-guide.md`); new tests (`test_log.lua`, `test_interface_log.lua`). Validated via `test_validate(lua_run)` — full suite 169/169 green; `luac` clean.

## [Completed] MPR-4 Performance / Addon-Compat / Nil-Hardening Engineer-Review Remediation (2026-06-13)
**Summary**: Third engineer-review pass, focused on per-frame performance, native-API-hooking addon compatibility, and Lua nil-error hardening, with adversarial validation before remediation. **Performance**: `ExperienceBar:Update` (ResourceOrbFrames) no longer does full per-frame work — it latches font/dimensions/anchors via an `ApplyStaticStyle` helper (mirroring `CastBar`/`MountStaminaBar`) and latches label text + value, skipping `SetText`/`UpdateVisuals` when unchanged, eliminating continuous per-frame string allocation + ~10 control mutations on the HUD frame loop; the orb numeric label `RefreshLabel` (`OrbVisuals.lua`) caches `_lastLabelText` and only `SetText`s when the bucketed string changes (was re-setting identical strings every animation frame during the 500ms post-power-change smooth transition). **Addon compatibility (native-API hooking)**: the tooltip `AddTopLinesToTopSection` PreHook (`GeneralInterface/Setup.lua`) that `return true`s to suppress native top-section content is now **scene-gated** — it suppresses only when BetterUI's enhancement actually renders, sharing the same `IsIncompatibleSceneActive` predicate the rendering hooks use (exposed from `Tooltips.lua`); previously it suppressed native + other addons' top-section content (incl. the set-collection tag) game-wide whenever the enhancement setting was on, now those are preserved outside BetterUI-enhanced contexts (PB-004 in-enhancement tag unaffected); ResourceOrbFrames `RegisterForUpdate` loop names are now `BETTERUI_`-prefixed (`OrbEvents.lua`), matching every other module for cross-addon collision-safety. **Lua nil-error hardening** (sibling-inconsistency / uncertain-API spots): `UltimateManager` coerces `GetUnitPower(...) or 0`; `OrbBarUpdates` coerces `GetUnitXP`/`GetUnitXPMax or 0`; `BankingActions:RefreshItemActions` guards `GetList()`/`itemActions`; `FooterManager:RefreshFooter` guards `footerFragment.control`; `QuantityDialog` narration guards `dialog.data`; `CompanionItemList` cooldown comparison nil-guards `GetItemCooldownInfo` returns; `SearchPresets` save-dialog guards `control.highlight`/`editBoxControl`. **Audit conclusions recorded (not bugs)**: native scene/method takeovers are intentionally not torn down on module disable — safe because module enable/disable is `requiresReload = true` (on reload-with-module-off, `Setup()` never runs and native is pristine); no runtime teardown path should be added. DI-trace wrappers in `DebugCommands.lua` replace `DIRECTIONAL_INPUT.Activate/Deactivate` only on the `/buidebug`/`/buiscene` debug commands and chain the original (opt-in, compat-safe). All test files green via `test_validate(lua_run, tools/tests/run_all_tests.lua)`.
**Source Issue/Phase**: MPR-4 engineer-review — per-frame HUD bar latching, scene-gated tooltip top-line suppression, `BETTERUI_`-prefixed update registrations, and nil-hardening of uncertain-API/sibling-inconsistency reads
**Related Files/Tests**: ResourceOrbFrames (`ExperienceBar`/HUD-bar `Update` + `ApplyStaticStyle`, `Core/OrbVisuals.lua` `RefreshLabel`, `SkillBar/UltimateManager.lua`, `OrbBarUpdates.lua`, `OrbEvents.lua`); GeneralInterface (`Setup.lua` tooltip top-line PreHook, `Tooltips/Tooltips.lua` exposed `IsIncompatibleSceneActive` predicate); Banking (`BankingActions:RefreshItemActions`, `FooterManager:RefreshFooter`, `QuantityDialog` narration); Companions (`CompanionItemList` cooldown comparison); TradingHouse (`SearchPresets` save-dialog); docs (`changelog.txt`, `tribal-knowledge.md`, `testing-guide.md`); new test (`test_orb_latch.lua` — drives real bar code, asserts no redundant `SetText`/`SetFont` on unchanged values) + extended (`test_tooltip_helpers.lua` — suppression hook returns true only in enhanced contexts, native top-lines preserved otherwise, PB-004 tag still appears). Validated via `test_validate(lua_run, tools/tests/run_all_tests.lua)` (full suite green).

## [Completed] MPR-3 ZO_-Native Drift Engineer-Review Remediation (2026-06-12)
**Summary**: Second engineer-review pass focused on ZO_-native drift in the shared CIM header/tab-bar path and module currency/batch flows, with adversarial validation before remediation. **CIM tab-bar dispatch**: `BETTERUI_TabBarScrollList:SetSelectedIndex` now dispatches the selection callback **exactly once** per change (was 2× in carousel mode / 3× in non-carousel mode) — the carousel `UpdateAnchors` is the single dispatcher (direct call gated to carousel mode; the `_zo_selectedDataChangedWrapper` covers non-carousel), used by every module header (Inventory/Banking/Vendor/Companions); `SetSelectedIndexWithoutAnimation` no longer mis-maps its suppression flag onto native's 3rd param `forceAnimation` (now passes `forceAnimation=false` and routes suppression to `UpdateAnchors`'s `blockSelectionChangedCallback`); `BETTERUI_Gamepad_ParametricList_Screen:Initialize` sets `self.headerContainer` for native parity (inherited `GetHeaderContainer()`); removed the dead, drifted `BETTERUI_HorizontalScrollList_Gamepad` class (live header path uses `BETTERUI_HorizontalParametricScrollList`); `BatchProcessor:Reset()` clears the stale `batchCallId` handle. **Vendor**: multi-select batch SELL / SELL_VENGEANCE pre-gates the gold cap — the batch stops with feedback instead of issuing N doomed `SellInventoryItem` calls (fence/launder unaffected). **Banking**: personal-bank currency transfer uses the modern `TransferCurrency(currencyType, amount, CURRENCY_LOCATION_*)` instead of the deprecated `WithdrawCurrencyFromBank`/`DepositCurrencyIntoBank` compat aliases (matches guild branch + current native); removed the dead write-only `self.loadingGuildBank` field. **Inventory**: `Class:Select` plays `SOUNDS.GAMEPAD_MENU_FORWARD` on category enter and registers `EVENT_CURRENCY_CAPS_CHANGED` for header refresh (native parity). **Companions**: corrected an inaccurate comment about the native BoE-equip dialog (`CONFIRM_EQUIP_ITEM` does exist; the custom queued dialog is the intentional choice). Deferred with rationale (see feature-requests): CooldownUtils 1500ms GCD threshold vs reading `GetSlotCooldownInfo`'s global return, TradingHouse guild-specific items, and `HasItemAtSlot`-vs-`IsSlotIdentityCurrent` for non-destructive transfer batches. All 166 test files green.
**Source Issue/Phase**: MPR-3 ZO_-native drift engineer-review — CIM tab-bar/header dispatch, vendor batch gold-cap, banking currency-transfer modernization, inventory category sound + currency-cap refresh, companions BoE comment
**Related Files/Tests**: CIM (`UI/` tab-bar/header scroll-list + `BETTERUI_Gamepad_ParametricList_Screen`, `Lists/BatchProcessor.lua`); Vendor (`Components/SellComponent.lua`, `Core/VendorBatchRuntime.lua`, `Core/List/BatchActionCounts.lua`); Banking (currency-transfer actions, `Banking.lua`); Inventory (`Inventory.lua` category `Select`, `Core/HeaderManager.lua`); Companions (`Actions/CompanionActions.lua` comment); docs (`changelog.txt`, `tribal-knowledge.md`, `testing-guide.md`, `feature-requests.md`); new test (`test_tabbar_selection_dispatch`) + extended vendor/banking/inventory/cim-list tests. Validated via `test_validate(lua_run, tools/tests/run_all_tests.lua)` (166 green).

## [Completed] MPR-2 Engineer-Review Remediation (2026-06-12)
**Summary**: Full-codebase engineer review (all ~383 Lua files) with adversarial finding validation, then routed remediation of all confirmed P0–P3 issues. Resolved the open P1/P2 backlog — PB-002 (Actions-dialog single-item "Mark as Junk" no longer kills LB/RB carousel paging: deferred keybind restoration to the post-dialog OnFinish path so the dialog's pushed keybind-state snapshot is not corrupted before Pop), PB-003 (disabling tooltip enhancements restores stock layout/fonts in-session by gating the per-layout PostHook and fully reversing fonts/anchors), PB-004 (enhanced tooltips re-add the set-collection Collected/Uncollected/Reconstructed tag), PB-005 (inventory pooled control names kept under the 63-char engine limit via `BETTERUI_GamepadInventoryTopLevel`→`BUI_GpInv` rename + category pool prefix), PB-006 (primary action re-resolves on in-place slot updates after container open). New confirmed fixes: PB-008 vendor batch "Buy All" excludes locked/ineligible entries; PB-010 batch sell/fence/launder/junk/lock re-validate slot identity at execution via the new shared `BETTERUI.CIM.Utils.CaptureSlotIdentity`/`IsSlotIdentityCurrent`; PB-009 Trading House Sell tab uses the engine authority `IsItemSellableOnTradingHouse` + `CanSellOnTradingHouse` guild gate; ORB-001 `GetSlotAbilityCost` ultimate-cost argument order. P3 hardening: TH off-scene guild-change `IsSceneShowing` guard, create-listing price-slider fine-step, SearchPresets version-stamp + SafeExecute, Companions destroy-eligibility `slotType`, ProtectionPolicy nil-slotType contract note, UnifiedScreen stale-reference fix, vendor skipped-step pacing + alt-currency icon. Cleanups: removed dead `VendorEventBridge.UnregisterCollectionUpdated`, the FoodBuffTracker stub + FoodBar control (HUD-002), and the Companions `CONFIRM_EQUIP_BOE` probe; HUD-003 Nameplates now consumes the canonical CIM Western-only font list. Adversarially refuted and dropped 11 false-positive findings (all 5 Banking flags, ORB-002, GEN-001, two CIM settings/contract claims, SafeExecute `ZO_Alert`). PLT-001 (file splits) and PLT-002 (locale backfill) reviewed and deferred with rationale (see feature-requests). All 165 test files green via `test_validate(lua_run, tools/tests/run_all_tests.lua)`.
**Source Issue/Phase**: MPR-2 deep engineer-review — PB-002, PB-003, PB-004, PB-005, PB-006, HUD-002, HUD-003 + new PB-008/PB-009/PB-010/ORB-001 findings
**Related Files/Tests**: CIM (`Core/Utilities.lua`, `Core/Batching/BatchActions.lua`, `MultiSelectManager.lua`, `Actions/ProtectionPolicy.lua`, `Core/Window/UnifiedScreen.lua`, `Core/Data/Types.lua`); Inventory (`Actions/ItemActionHandlers.lua`, `Core/HeaderManager.lua`, `Core/Utils.lua`, `Inventory.lua`, `Lists/CategoryListManager.lua`, `Module.lua`, `Templates/GamepadInventory.xml`, `UI/TooltipEquipped.lua`, `UI/TooltipUtils.lua`); GeneralInterface (`Tooltips/Tooltips.lua`, `Tooltips/Settings.lua`); Vendor (`Core/List/BatchActionCounts.lua`, `Core/VendorBatchRuntime.lua`, `Components/SellComponent.lua`, `Core/List/VendorRowSetup.lua`, `Core/Lifecycle/VendorEventBridge.lua`); TradingHouse (`Components/SellComponent.lua`, `Core/TradingHouseRuntimeFlow.lua`, `Core/SearchPresets.lua`); Companions (`Core/CompanionItemList.lua`, `Actions/CompanionActions.lua`); ResourceOrbFrames (`SkillBar/UltimateManager.lua`, `FrontBarManager.lua`, `FrontBarPressFeedback.lua`, `Core/OrbBars.lua`, `OrbBarUpdates.lua`, `ResourceOrbFrames.lua`, `Templates/ResourceOrbFrames.xml`, `LayoutTemplates.xml`); Nameplates (`Nameplates.lua`); `.luarc.json`; docs (`changelog.txt`, `tribal-knowledge.md`, `architecture.md`, `testing-guide.md`); new tests (`test_inventory_junk_carousel_keybinds`, `test_inventory_primary_action_inplace_update`, `test_inventory_control_name_length`, `test_trading_house_sell_filter`) + extended (`test_batch_safety`, `test_inventory_destroy_policy`, `test_tooltip_helpers`, `test_trading_house_callbacks`, `test_trading_house_search_presets`, `test_vendor_batch_action_counts`, `test_vendor_batching_parallel_contract`, `test_front_bar_manager`, `test_companion_actions_source`, `test_companions_runtime_source`, `test_nameplates_reset`, + load-order fixes).

## [Completed] ECO-002 Vendor Phase 1 Closeout (2026-04-16)
**Summary**: Closed the remaining vendor phase 1 backlog by finishing stable riding-training progress bars and adding SellVengeance support. Stable training rows now use a dedicated BetterUI template with inline progress bars, and the vendor scene now exposes a BetterUI Sell Vengeance tab backed by `BAG_VENGEANCE` when the Vengeance ruleset and sell flag are active.
**Source Issue/Phase**: ECO-002 Vendor phase 1 (`VND-002`, `VND-003`)
**Related Files/Tests**: `Modules/CIM/Constants.lua`, `Modules/CIM/Templates/SharedTemplates.xml`, `Modules/Vendor/Core/BatchActionCounts.lua`, `Modules/Vendor/Core/VendorClass.lua`, `Modules/Vendor/Core/VendorRowSetup.lua`, `Modules/Vendor/Components/SellVengeanceComponent.lua`, `Modules/Vendor/Vendor.lua`, `lang/en.lua`, `BetterUI.txt`, `tools/tests/test_vendor_sell_vengeance_source.lua`, `tools/tests/test_vendor_stable_progress_source.lua`, `tools/tests/test_vendor_stable_transition.lua`, `tools/tests/test_vendor_stable_icons.lua`, `tools/tests/test_vendor_tabs.lua`

## [Completed] March 14 Completion Audit + Feature Gap Fixes (2026-04-11)
**Summary**: Audited all March 14, 2026 completion claims against actual codebase. Identified 5 partially-implemented features and 2 missing planning docs. Fixed all gaps: integrated StatComparison into Banking and Companion surfaces (INV-001 parity), added narration registration for Vendor and TradingHouse scenes (ACC-001 parity), implemented Trading House search presets (TH-001), documented ORB_CONFIG table, and removed dead planning doc references (continuity-ledger.md, console-readiness.md). PLT-001 console readiness postponed.
**Source Issue/Phase**: Post-completion audit of March 14 items
**Related Files/Tests**: `Modules/Inventory/Core/StatComparison.lua` (equipBagId param), `Modules/Banking/Lists/BankRowSetup.lua` (stat comparison), `Modules/Companions/Core/CompanionItemList.lua` (stat comparison), `Modules/Vendor/Vendor.lua` (narration), `Modules/TradingHouse/TradingHouse.lua` (narration + preset keybinds), `Modules/TradingHouse/Components/SearchPresets.lua` (new), `Modules/TradingHouse/Module.lua` (searchPresets default), `Modules/ResourceOrbFrames/Core/OrbVisuals.lua` (ORB_CONFIG docs), `docs/README.md`, `docs/planning/completed-improvements.md`, `BetterUI.txt`

## [Completed] Second-Pass Audit Closeout + Language Maintenance Fix (2026-03-28)
**Summary**: Performed requested second-pass deep verification and fixed additional misses: scene lifecycle task-manager routing/cancellation for Banking and Inventory, stale CIM template icon path, and strict-mode PowerShell `$Matches` collision in localization maintenance tooling.
**Source Issue/Phase**: Post-refactor follow-up audit pass
**Related Files/Tests**: `Modules/Banking/Banking.lua`, `Modules/CIM/Core/Window/WindowClass.lua`, `Modules/Inventory/Scene/InventorySceneLifecycle.lua`, `Modules/CIM/Templates/SharedTemplates.xml`, `Modules/ResourceOrbFrames/Core/OrbEvents.lua`, `tools/LanguageMaintenance.ps1`, `luacheck BetterUI.lua Modules lang tools/tests`, `lua tools/tests/run_all_tests.lua`, `mcp_test-runner_test_validate(luac_syntax)`, `pwsh -File tools/LanguageMaintenance.ps1 -Mode Audit`

## [Completed] Refactor Audit + Manifest Integrity Pass (2026-03-28)
**Summary**: Completed deep post-refactor audit from `99ac62a41af2d1e176ced6a5a221f5ab190192fd` to `HEAD`; validated Lua syntax and XML structure, verified manifest coverage/order, and fixed manifest/quality issues discovered during audit.
**Source Issue/Phase**: Post-refactor verification sweep
**Related Files/Tests**: `BetterUI.txt`, `Modules/GeneralInterface/Tooltips/Tooltips.lua`, `Modules/Inventory/Module.lua`, `Modules/ResourceOrbFrames/ResourceOrbFrames.lua`, `docs/publishing/changelog.txt`, `luacheck BetterUI.lua Modules lang tools/tests`, `lua tools/tests/run_all_tests.lua`, `mcp_test-runner_test_validate(luac_syntax)`

## [Completed] Guild Bank Validation and Commit (2026-03-14)
**Summary**: Implemented comprehensive Guild Bank support via UI reuse. Fixed deposit restriction missing check for player-locked items and matched `TransferToGuildBank`/`TransferFromGuildBank` exact argument signatures.
**Source Issue/Phase**: Guild Bank Integration
**Related Files/Tests**: `TransferActions.lua`, `GuildBankAdapter.lua`, `MultiSelectActions.lua`, `BankListManager.lua`, `KeybindManager.lua`

## March 14, 2026

- `BUI-P1-001` / `project-improvements.md` - Complete multi-select anti-spam hardening rollout (Phases 0-5)
- `BUI-P1-002` / `ACC-001` - Finish gamepad narration parity for BetterUI custom surfaces
- `BUI-P1-003` / `INV-003` - Restore reliable "New Item" lifecycle and clear behavior in inventory flows
- `BUI-P1-004` / `BNK-001` - Define and begin guild-bank integration path for BetterUI Banking
- `INV-001` - Item stat comparison parity across Inventory, Banking, and Companion item surfaces
- `INV-004` - Companion equipment management workspace with comparison and slot views
- `ECO-002` - Vendor/store enhancements (sorting, price context, batch junk sell UX)
- `TH-001` - Guild store/trading house overhaul with stronger search, presets, and unit-price ergonomics
- XML Template Audit - `[Modules/CIM/ConstantsUI.lua]`
- v3.2 Compat Removal - `[Modules/CIM/ConstantsUI.lua]`
- EmmyLua Typing - `[Modules/CIM/Core/Utilities.lua]`
- Config Documentation - `[Modules/ResourceOrbFrames/Core/OrbVisuals.lua]`
- Tribal Knowledge Gotchas - `[docs/reference/tribal-knowledge.md]`
- `TODO-09` Bar Constants Refactoring - Migrated 30+ global bar constants to `BETTERUI.ResourceOrbFrames.CONST.BARS` namespace, cleaned stale `.luarc.json` entries
- `DeveloperDebug.lua` Split - Split from 627 lines to 114 lines by extracting BatchConfig, BatchOverlay, and BatchActions helpers
- Anti-Spam Implementation Plan - All 15 items verified complete (re-entry guard, pipeline token, weighted cost, adaptive delay, jitter, rate limiting, chunk cooldowns, post-batch cooldown, destination slot)
- Batch Safety Test Coverage - `test_batch_safety.lua` covering re-entry guard, pipeline token invalidation, and adaptive backoff


## [Completed] BUI-TRACE-001 — AI-replay builog foundation, settings coverage, and audit-finding drain (2026-07-01)
**Summary**: Reconciliation audit (2026-07-01) confirmed the bulk of BUI-TRACE-001 landed across the 2026-06-20 → 2026-06-30 builog commit series while the plan text had gone stale (all boxes unchecked). Verified complete in source:
- **Phase 1 (trace contract foundation)**: `Log.TraceEvent` envelope (`event=/phase=/traceVersion/eventName/phaseName`), `sid`/`seq` identity, flow envelopes (`FlowBegin`/`FlowEnd`), per-line watch context (`scene/view/flow/lastAction`), serializers (`DescribeItem` incl. vendor/TH `entryIndex`/`tradingHouseIndex`/`listingIndex`, `DescribeKeybindDescriptors`, `DescribeListSelection`, `Summarize`), dropped-record visibility (`dropped=N reason=rate_limit` + heartbeat `dropped=` field).
- **Phase 2 (settings/LAM coverage)**: `SettingsAccessor` traces `settings.value` `read`/`write_before`/`write_after`/`callbacks_fired`/`write_rejected`; `SettingsFactory` instruments LAM `getFunc`/`setFunc`/`func`/`disabled`/`warning`; root panel + module toggles traced; `/builog` setting writes traced via `settings.builog`.
- **Phase 3 (inventory/banking replay paths)**: transfer request/blocked/deny instrumentation (incl. guild-bank deny via `WarnBankTransferBlocked`), currency `before/settled` snapshots (`settledFrom`/`settledTo`), pending mark/clear, dialog lifecycle, keybind flow logging.
- **Phase 5 (partial, visual/interface modules)**: `Watch.RegisterSnapshotProvider` registrations live in all 8 modules (banking, inventory, vendor, tradingHouse, writs, companions, nameplates, resourceOrbs, generalInterface); ResourceOrbFrames + Vendor scene-lifecycle trace coverage (2026-06-26 commits).
- **Audit-finding drain (verified fixed in source 2026-07-01)**: keybind label-getter TRACE spam removed; `inventory update observed` batch spam removed; bank currency hidden-skip moved to TRACE with state payloads; companion batch-dialog nil-deref guarded; companion split-stack `result` reflects `didShowDialog`; companion trace helpers call `SetLastAction`; companion action eligibility reasons via `ResolveActionEligibility`; `WriteRaw` pipe handling confirmed correct-as-is (neutralizing would destroy the ` | ` parse boundary — pass-2 finding #3 rejected).

Remaining verified gaps (outcome confirmation, async flow propagation, taxonomy standardization, privacy mode, transport hardening, module envelope completion) were re-scoped into **BUI-TRACE-002** in `project-improvements.md`.
**Source Issue/Phase**: BUI-TRACE-001 (superseded by BUI-TRACE-002)
**Related Files/Tests**: `Modules/CIM/Core/Diagnostics/{Log,InterfaceLog,WatchMode,SceneLog,Screenshot}.lua`, `Modules/CIM/Core/Settings/{SettingsAccessor,SettingsFactory}.lua`, `Modules/Banking/Actions/TransferActions.lua`, `Modules/Banking/Keybinds/KeybindManager.lua`, `Modules/Companions/**`, module snapshot-provider registrations, `tools/tests/test_log.lua`, `test_interface_log.lua`, `test_log_parse_contract.lua`, `test_builog_flow_source.lua`, `test_builog_monitor_source.lua`, `test_watchmode.lua`, `docs/reference/builog-developer-guide.md`

## [Completed] Comment Feedback Implementation Plan — All Phases (2026-06-10)
**Summary**: Implemented all four phases from `docs/planning/comment-feedback-implementation-plan.md`:

- **PB-007** — Vault deposit busy-state keybind: Added `_pendingTransfers` table with 5s timeout, `MarkTransferPending`/`ClearTransferPending`/`IsTransferPending`/`SweepStaleTransfers`, and `EVENT_INVENTORY_SINGLE_SLOT_UPDATE` auto-clear. Deposit keybind now disables while a transfer is in-flight for the selected slot.
- **ECO-001** — Archival Fortunes currency display: Added 13th currency entry to `CURRENCY_DATA`, `CurrencyManager.DEFS`, all four `CURRENCY_PRESETS`, order clamp (12→13), `POS_13` choices, and guarded with `rawget(_G, "CURT_ARCHIVAL_FORTUNES")` for API safety.
- **TRC-001** — Market prices on crafting/improvement tooltips: Created `CraftingPriceTooltip.lua` with `ZO_PostHook` for `LayoutPendingSmithingItem` and `LayoutImproveResultSmithingItem`; reuses `MarketIntegration.GetMarketPriceInfo`; gated by `showCraftingMarketPrice` setting (default on).
- **HUD-001** — Independent orb positioning: Added `enableIndependentOrbOffset`, `orbOffsetX`, `orbOffsetY` to ResourceOrbFrames defaults; modified `UpdateOrbLayout` to apply offsets to left/right orb anchors in all branches; added LAM checkbox + X/Y sliders.

**Source Issue/Phase**: `docs/planning/comment-feedback-implementation-plan.md` (Phases 1–4)
**Related Files/Tests**:
- PB-007: `Modules/Banking/Actions/TransferActions.lua`, `Modules/Banking/Keybinds/KeybindManager.lua`, `tools/tests/test_banking_transfer_actions.lua`
- ECO-001: `Modules/Inventory/Settings/CurrencySettings.lua`, `Modules/CIM/UI/CurrencyManager.lua`, `Modules/CIM/Constants.lua`, `lang/*.lua`
- TRC-001: `Modules/GeneralInterface/Tooltips/CraftingPriceTooltip.lua`, `Modules/GeneralInterface/Tooltips/Settings.lua`, `Modules/CIM/Core/Settings/DefaultsRegistry.lua`, `Modules/CIM/Core/Settings/SettingsMetadata.lua`, `BetterUI.txt`, `lang/*.lua`, `tools/tests/test_crafting_price_tooltip.lua`
- HUD-001: `Modules/ResourceOrbFrames/Core/OrbVisuals.lua`, `Modules/ResourceOrbFrames/Module.lua`, `Modules/ResourceOrbFrames/Settings/Defaults.lua`, `lang/*.lua`, `tools/tests/test_orb_independent_positioning.lua`
